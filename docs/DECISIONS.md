# Architecture Decision Log

This append-only log records rationale and history. `ARCHITECTURE.md = current canonical contract`; `DECISIONS.md = append-only rationale/history` and does not override it.

## DEC-0001 — Event → Command correlation

- **Date:** 2026-08-20
- **Status:** Accepted
- **Context:** Event causality alone did not link all results of one logical Command transaction.
- **Decision:** Every domain Event has mandatory `commandId`. All Events from one Command share it, including cascading Events. System actions enter the authoritative pipeline through a system/internal Command. Canonical envelope: `eventId`, `commandId`, `type`, `version`, `campaignId`, `timestamp`, `actorId`, `causedBy`, `payload`.
- **Consequences:** tracing, replay, and audit can reconstruct a Command transaction; `causedBy` remains Event → Event causality.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§8–9.

## DEC-0002 — Runtime IDs do not depend on Definition IDs

- **Date:** 2026-08-20
- **Status:** Accepted
- **Context:** Definition-derived runtime IDs blur immutable content and campaign State.
- **Decision:** Use generic runtime prefixes: `character_NNN`, `player_NNN`, `npc_NNN`, `monster_NNN`, `item_NNN`, `combat_NNN`, `quest_NNN`, `objective_NNN`, `location_NNN`, `effect_NNN`, `condition_NNN`, `command_NNNNNN`, `event_NNNNNN`. Store Definition separately, e.g. `monster_001` with `definitionId: goblin`. Player identity and CharacterState remain distinct.
- **Consequences:** Definitions may retain semantic IDs such as `goblin`, `fighter`, and `longsword`; runtime identity stays stable.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §4.

## DEC-0003 — Game time belongs to World State

- **Date:** 2026-08-20
- **Status:** Accepted
- **Context:** Campaign ownership of global game time created two logical owners.
- **Decision:** `WorldEngine / WorldState` is the sole authoritative owner of world/game time. Campaign ownership covers identity, ruleset reference, metadata, and session/lifecycle metadata. Event system timestamps are separate.
- **Consequences:** one State has one logical owner.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §10.

## DEC-0004 — Pydantic only at boundaries

- **Date:** 2026-08-20
- **Status:** Accepted
- **Context:** Domain contracts needed a framework-independent model.
- **Decision:** Domain uses dataclasses, frozen dataclasses for Definitions and immutable Value Objects, Enums, Value Objects, and type hints. Pydantic is restricted to API, storage I/O validation, configuration, external JSON, and LLM structured output.
- **Consequences:** rule resolution has no Pydantic dependency.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§1.2, 12.7.

## DEC-0005 — Phase 0 includes minimal CI

- **Date:** 2026-08-20
- **Status:** Accepted
- **Context:** Foundation must be technically verifiable.
- **Decision:** GitHub Actions uses Python 3.12, `python -m pip install -e ".[dev]"`, and `python -m pytest` on push and pull requests.
- **Consequences:** no lint, type, format, or coverage tools are introduced.
- **Affected contracts/files:** `.github/workflows/tests.yml`, `docs/ROADMAP.md`.

## DEC-0006 — JSON placeholder policy

- **Date:** 2026-08-20
- **Status:** Accepted
- **Context:** zero-byte JSON files are neither data nor valid JSON artifacts.
- **Decision:** A tracked `.json` must be non-empty valid JSON; otherwise it is not created. Empty directories use `.gitkeep`. An empty `events.jsonl` is allowed as a valid empty append-only stream.
- **Consequences:** repository tests validate all JSON artifacts under `rules/` and `campaigns/`.
- **Affected contracts/files:** `rules/`, `campaigns/`, `tests/test_json_artifacts.py`.

## DEC-0007 — README separates current and planned state

- **Date:** 2026-08-20
- **Status:** Accepted
- **Context:** README implied a runnable API and installed dependencies that do not yet exist.
- **Decision:** README lists Python 3.12+, JSON/JSONL, pytest, and setuptools as current Foundation; FastAPI, WebSocket, Pydantic v2, and LLM adapters are planned boundary stack.
- **Consequences:** planned dependencies wait for their Roadmap phase.
- **Affected contracts/files:** `README.md`, `docs/ROADMAP.md`.

## DEC-0008 — Phase 1 starts with minimal Core data contracts

- **Date:** 2026-08-22
- **Status:** Accepted
- **Context:** Phase 1 needed canonical first-pass Value Object, Definition, and Creature State shapes before Python implementation, including one unambiguous HP field name.
- **Decision:** `AbilityScores` is an immutable six-score Value Object whose required values are each in `1..30`. `CreatureState` uses `current_hp` / `max_hp` in Python (`currentHp` / `maxHp` in JSON). `ItemDefinition`, `WeaponDefinition`, and `MonsterDefinition` contain only their documented minimal Phase 1 fields, and `WeaponDefinition` is a specialization of `ItemDefinition`. Runtime State remains separate from immutable Definitions.
- **Consequences:** fields belonging to later Roadmap phases are intentionally absent; future slices can extend the canonical contracts only when the corresponding behavior is implemented.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§1.2.1, 3.1.1, 3.2.1.

## DEC-0009 — DamageType is a closed Domain StrEnum

- **Date:** 2026-08-22
- **Status:** Accepted
- **Context:** DEC-0008 and the initial Phase 1 `WeaponDefinition` contract deferred the exact `damage_type` Domain type and closed set until code implementation.
- **Decision:** `DamageType` is a Domain `StrEnum` with exactly 13 lowercase string values: `acid`, `bludgeoning`, `cold`, `fire`, `force`, `lightning`, `necrotic`, `piercing`, `poison`, `psychic`, `radiant`, `slashing`, and `thunder`. `WeaponDefinition.damage_type` uses `DamageType`, and future serialization emits the enum string value.
- **Consequences:** weapon definitions cannot extend the canonical damage-type vocabulary with arbitrary enum members. This decision introduces no resistance, immunity, vulnerability, or damage-calculation mechanics; those remain deferred to their corresponding future Roadmap phases.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.1.1, 12.16; `src/dnd_engine/domain/value_objects/damage_type.py`; `src/dnd_engine/domain/definitions/weapon.py`.

## DEC-0010 — Minimal Phase 1 CampaignState contract

- **Date:** 2026-08-22
- **Status:** Accepted
- **Context:** Phase 1 reached `CampaignState`. Architecture already assigned Campaign ownership but did not define a concrete minimal Python schema, leaving room for the implementation to become an implicit aggregate or God Object for other State domains.
- **Decision:** Phase 1 `CampaignState` is mutable and contains exactly `id: str`, `ruleset_id: str`, and `ruleset_version: str`. Other State domains are not embedded in it. World/game time remains exclusively in World State. Concrete fields for campaign metadata, session state, and lifecycle are deferred until corresponding use cases and contracts exist.
- **Consequences:** Campaign State remains small while exposing campaign identity and an explicit ruleset identity/version reference. State Ownership boundaries remain intact. A persistence snapshot may aggregate State domains separately without transferring their ownership to `CampaignState`. Future extensions require a concrete use case and canonical contract update.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §3.2.2; `src/dnd_engine/domain/state/campaign.py`.

## DEC-0011 — Minimal Phase 1 Dice Engine contract

- **Date:** 2026-08-22
- **Status:** Accepted
- **Context:** Phase 1 reached Dice Engine. Architecture already required all gameplay randomness to pass through `DiceEngine` and showed `roll("1d20")`, but the exact Domain API, result type, minimal parsing ownership, and RNG injection contract were not canonical.
- **Decision:** `DiceEngine` is a Domain `Protocol` with `roll(expression: str) -> DiceRoll`. Immutable `DiceRoll` contains the accepted `expression`, individual `rolls`, and their `total`. Phase 1 accepts strict simple lowercase `NdM` only; modifiers and a full DSL are deferred. Parsing remains private to the Infrastructure implementation. Production `PythonDiceEngine` receives an injected `random.Random`; Domain does not depend on Python RNG implementation or Infrastructure. RNG internal state is not authoritative campaign State.
- **Consequences:** Rules can receive deterministic scripted `DiceEngine` implementations, production randomness remains replaceable, and equivalent controlled RNG sources can reproduce sequences. Individual rolls remain available for debugging and future resolution/replay work. No Phase 2 rules or replay subsystem are introduced.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §1.7.1; `src/dnd_engine/domain/services/dice.py`; `src/dnd_engine/domain/value_objects/dice_roll.py`; `src/dnd_engine/infrastructure/random/dice.py`.

## DEC-0012 — Minimal Phase 1 Event model contract

- **Date:** 2026-08-23
- **Status:** Accepted
- **Context:** Phase 1 reached the Event model. The canonical Event Envelope already defined identity, Command correlation, campaign scope, causation, naming, and schema-version semantics, but its Domain timestamp representation, payload immutability, nullable-field codec behavior, and the boundary included in this slice required clarification.
- **Decision:** Phase 1 uses one generic immutable `GameEvent`; no separate Python `EventEnvelope` type is introduced. Its timestamp is an explicitly supplied timezone-aware UTC `datetime`, serialized as ISO 8601 UTC with canonical `Z`. Its generic JSON-compatible payload is defensively and recursively immutable. `actorId` and `causedBy` are always emitted and serialize Domain `None` as JSON `null`; missing or null input deserializes to `None`. A pure `EventSerializer` is included in this slice.
- **Consequences:** Event values cannot read the clock, allocate their identity, mutate State, or be changed through nested payload references. EventStore, Event ID/sequence allocation, filesystem persistence, State application, replay, and concrete gameplay Event types remain deferred.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.4, 8.7–8.10, 12.10, 12.17; `src/dnd_engine/domain/events/game_event.py`; `src/dnd_engine/infrastructure/persistence/json/event_serializer.py`.

## DEC-0013 — Minimal Phase 1 State Store contract

- **Date:** 2026-08-24
- **Status:** Accepted
- **Context:** Phase 1 reached State Store. The canonical architecture required versioned State snapshots but did not yet define the minimal persistence aggregate, strict v1 codec, filesystem error boundary, or atomic replacement behavior without prematurely coupling snapshot persistence to the future EventStore.
- **Decision:** `StateStore` is a snapshot-only Domain port with `load(campaign_id)` and `save(snapshot)`. Frozen `StateSnapshot` groups one `CampaignState` and `tuple[CreatureState, ...]` for persistence without becoming a State Owner or transferring Creature ownership to Campaign. Pure `StateSerializer` maps that aggregate to the exact camelCase Phase 1 `state.json` structure with integer `schemaVersion = 1`, outer `campaignId`, nested Campaign fields, and a deterministically ID-sorted Creature array. Deserialization is strict: all fields are required, unknown fields/defaults/coercion are forbidden, the campaign IDs must match, and nested Domain invariants and unique Creature IDs are enforced. `FilesystemStateStore` uses UTF-8 deterministic JSON at `<root>/<campaign_id>/state.json` and atomically replaces the single file using a same-directory temporary file plus `os.replace` under a Phase 1 single-writer assumption. `schemaVersion` is not a State revision.
- **Consequences:** State snapshot persistence remains separate from gameplay rules and Event persistence. EventStore, replay, Event-to-State application, transaction ordering between EventStore and State projection, revisions/locking, migrations, and database implementations remain deferred. No production dependency is added.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.2.3, 12.9, 12.12; `src/dnd_engine/domain/state/snapshot.py`; `src/dnd_engine/domain/services/state_store.py`; `src/dnd_engine/infrastructure/persistence/json/state_serializer.py`; `src/dnd_engine/infrastructure/filesystem/state_store.py`.

## DEC-0014 — Phase 1 closure and minimal Phase 2 resolution foundation

- **Date:** 2026-08-24
- **Status:** Accepted
- **Context:** All Phase 1 Roadmap items were implemented and tested, while README/Roadmap status still named Phase 1 as current. Before the first Phase 2 vertical slice, the canonical JSON Command Envelope, future Python Command representation, `ResolutionResult.success`, `state_changes`, expected errors, Ability identifiers, resolver/application responsibilities, and orchestration scope required an unambiguous boundary.
- **Decision:** Phase 1 implementation is complete and the repository moves to Phase 2 — Basic Rules without marking any Phase 2 mechanic complete. The canonical five-field Command Envelope is retained. Validated gameplay Python Commands will be concrete typed immutable dataclasses with concrete typed payloads and fixed command types, not a generic dict-based Domain Command hierarchy; the first planned forms are `AbilityCheckCommand` and `AbilityCheckPayload`. The future immutable generic `ResolutionResult[T]` contains exactly `success`, `command_id`, `outcome`, tuple `rolls`, tuple `events`, and tuple `errors`; `success` means processing/rule-resolution success, not gameplay success. No `state_changes` field or placeholder `StateChange` abstraction is materialized before a concrete state-mutating use case; authoritative mutation remains Event-driven. Expected processing failures use the planned minimal `ErrorCode` / immutable `EngineError` representation, while intrinsic invalid construction may use `TypeError`/`ValueError` and infrastructure/programming failures are not automatically gameplay errors. `Ability` is a planned closed lowercase `StrEnum`; ability modifier is the pure derived rule `(score - 10) // 2`, not stored in `AbilityScores` or State. The Ability Check resolver is stateless, receives a typed Command, `CreatureState`, and `DiceEngine`, performs one `1d20` roll, and does not load/save State or create Event metadata. State lookup and actor validation belong to Application. Ability Check is read-only, so its handler loads State but does not save it. After successful resolution Application creates the planned `AbilityCheckResolved` generic `GameEvent`, with gameplay success/failure in payload. `GameEngine`, `GameContext`, EventStore, buses, registries, dispatcher, transaction coordinator, replay, and broader orchestration framework remain deferred until multiple concrete use cases demonstrate shared behavior.
- **Consequences:** The first Phase 2 implementation can be a narrow explicit Application handler plus pure Domain resolver without accepting untyped gameplay payloads or prematurely designing a framework. A failed check can have `ResolutionResult.success is True` and `outcome.succeeded is False`; a processing failure has no outcome or Events and contains structured errors. Phase 1 contracts and runtime behavior remain unchanged.
- **Affected contracts/files:** `README.md`; `CLAUDE.md`; `docs/ARCHITECTURE.md` §§2.2–2.3, 2.6, 3.3, 3.5–3.10, 9.6; `docs/ROADMAP.md`; `docs/DECISIONS.md`; `docs/DEVELOPMENT_LOG.md`.

## DEC-0015 — Single canonical Command lifecycle

- **Date:** 2026-08-24
- **Status:** Accepted
- **Context:** The canonical architecture described the Command lifecycle twice with two incompatible state vocabularies. §3.3 used `Created → Validating → Valid | Invalid → Executing → Completed | Failed`, while §9.7 used `Created → Validating → Rejected | Accepted → Resolving → Completed | Failed`. `CLAUDE.md` reproduced a third variant whose initial state `Received` appeared in neither. No Command is implemented, so no code, test, or persisted data depended on either vocabulary.
- **Decision:** §9.7 is the single canonical description of the Command lifecycle, with the states `Created`, `Validating`, `Rejected`, `Accepted`, `Resolving`, `Completed`, and `Failed`. The `Valid` / `Invalid` / `Executing` vocabulary is rejected. The lifecycle is documented in exactly one section: §3.3 no longer restates it and links to §9.7 instead.
- **Consequences:** The two descriptions can no longer drift apart, because only one of them exists. `Rejected` stays aligned with the rejection reasons listed in §9.7 and with `ErrorCode` in §3.9. No Python contract, implementation, test, or runtime behavior changes. `CLAUDE.md` still carries the stale third variant and is corrected in a separate slice.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.3, 9.7; `docs/DECISIONS.md`.

## DEC-0016 — CLAUDE.md duplication policy

- **Date:** 2026-08-24
- **Status:** Accepted
- **Context:** `CLAUDE.md` is loaded automatically by the coding agent while `docs/ARCHITECTURE.md` is not, so the summary had grown verbatim copies of canonical structures: the full ID reference table, the full State Ownership matrix, both Envelope JSON examples, and Definition field lists. DEC-0015 had just removed a duplicated Command lifecycle for exactly this reason, and `CLAUDE.md` still carried a third, invented lifecycle vocabulary whose initial state `Received` existed nowhere in the canon. At the same time the summary omitted every implemented Phase 1 contract, the phase-scope rule, the deferred-abstraction list of §3.6, the mandatory `docs/DEVELOPMENT_LOG.md` append rule, the document map, and branch/PR policy. Without a stated rule for what the summary may repeat, every future edit re-opens the question and the file drifts again.
- **Decision:** `CLAUDE.md` reproduces verbatim only single facts that an agent can violate without noticing: names, closed value sets, phase status, and prohibitions. Structures — reference tables, dataclass field lists, and JSON envelope examples — are not reproduced; `CLAUDE.md` names the entity and points to its canonical section instead. A dedicated naming-trap section enumerates the specific points where an agent's default intuition diverges from the canon. `CLAUDE.md` is a summary for the agent and never an alternative contract: on any conflict with `docs/ARCHITECTURE.md`, including one introduced by a task instruction, the agent stops and reports instead of choosing.
- **Consequences:** Canonical structures have exactly one location, so a later change to §4.13, §10.13, §8.1, §9.1, or a Phase 1 field list no longer requires a matching edit in `CLAUDE.md`. The agent must open `docs/ARCHITECTURE.md` to obtain field lists and envelope schemas; the summary alone is not sufficient for contract-level work. The stale `Received` lifecycle variant left open by DEC-0015 is removed. This decision changes no Python contract, no test, and no runtime behavior.
- **Affected contracts/files:** `CLAUDE.md`; `docs/DECISIONS.md`.

## DEC-0017 — Campaign ID uses the strict numeric format

- **Date:** 2026-08-24
- **Status:** Accepted
- **Context:** `campaignId` is a required field of both the Command and Event Envelope and, per §12.9, is also the campaign directory name, yet the §4.13 ID Reference Table listed every entity except Campaign. §4.12 registered the instance `campaign_001` but no section stated the format, and §4.2 does not list Campaign among its examples of the generic instance pattern. Because §4.12 explicitly permits semantic IDs for Quest and Location, the absence of any statement for Campaign left semantic campaign IDs such as `curse_of_strahd` neither allowed nor forbidden, and `CLAUDE.md` directed the agent to §4.13 for all ID formats.
- **Decision:** Campaign ID uses the strict numeric instance format `campaign_NNN`, consistent with the generic pattern in §4.2. Semantic Campaign IDs are forbidden. The prohibition is stated in §4.12 alongside the registered instance, and the format is listed in §4.13 as a row placed after Definition and before Character, first among the runtime IDs. A human-readable campaign title is not encoded in the identifier.
- **Consequences:** `campaignId` is now validatable by a single pattern everywhere it appears — both Envelopes, the state snapshot, and the campaign directory name — and remains stable when a campaign is renamed, as §4.9 requires of every ID. The cost is that campaign directories are not self-describing on disk; a human-readable title belongs in state rather than in the identifier. §4.2 still omits Campaign from its example list, §4.10 states no uniqueness scope for Campaign, and §4.11 assigns no service that generates Campaign IDs; these gaps are recorded here and left open. This decision changes no Python contract, no test, and no runtime behavior.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §4.12, §4.13; `docs/DECISIONS.md`.

## DEC-0018 — CLAUDE.md is resynchronised whenever the canon changes

- **Date:** 2026-08-24
- **Status:** Accepted
- **Context:** DEC-0016 fixed which canonical facts `CLAUDE.md` reproduces and which it replaces with a section reference, but it assigned no obligation to reread the summary when the canon changes. `CLAUDE.md` originally drifted for exactly that reason: nobody owned it, and the agent read `AGENTS.md` instead. Two parts of the rebuilt summary remain exposed, the table of implemented Phase 1 contracts and the naming-traps table, because both restate facts that a future contract change could invalidate silently. Separately, `AGENTS.md` stated the commit, push, pull request, and merge authorisation rules twice, in "Branches and pull requests" and again in "Change authorisation and diff review", which is the same duplication hazard inside the process document.
- **Decision:** `AGENTS.md` now requires rereading `CLAUDE.md` after any canonical contract change and updating it within the same slice if a reproduced fact changed, and names the classes of fact the summary reproduces so the check is actionable. Authorisation for commit, push, pull request creation, and merge is stated only in "Change authorisation and diff review"; "Branches and pull requests" keeps branch hygiene and pull request description requirements and carries a cross-reference instead of a restatement.
- **Consequences:** DEC-0016 is complete: the summary now has both a defined content policy and a defined maintenance trigger. Contract slices cost slightly more, since each one must check the summary even when it turns out to be unaffected. The check is manual and nothing enforces it; if it is skipped, drift returns silently, which is the failure this decision is meant to make visible rather than impossible. This decision changes no Python contract, no test, and no runtime behavior.
- **Affected contracts/files:** `AGENTS.md`; `docs/DECISIONS.md`.

## DEC-0019 — Ability Check result and Event metadata boundaries

- **Date:** 2026-08-24
- **Status:** Accepted
- **Context:** DEC-0014 prepared the first Phase 2 Ability Check slice, but its planned `ResolutionResult[T]` duplicated each roll at the generic result level, the Application responsibility to create a `GameEvent` conflicted with Event ID allocation being assigned to the deferred EventStore, and the concrete `AbilityCheckResolved` payload/builder contract was not fixed.
- **Decision:** Application handlers obtain `EventMetadata` from one injected application-facing `EventMetadataProvider`; handlers neither allocate Event IDs nor read the system clock directly, and UI/AI/API are not authoritative metadata sources. The provider is not an EventStore and provides no durability guarantee. The future immutable `ResolutionResult[T]` contains exactly `success`, `command_id`, `outcome`, tuple `events`, and tuple `errors`; generic top-level `rolls` is removed. A successful result has a non-null outcome and no errors. A failed result has no outcome or Events and at least one error. Every returned Event has the result's `command_id`; successful results are not generically required to contain an Event. Gameplay failure remains successful processing. The generic immutable `GameEvent` envelope is retained without a concrete Event hierarchy, registry, upcasters, or EventBus. `AbilityCheckResolved` uses `type="AbilityCheckResolved"`, `version=1`, the typed `AbilityCheckResolvedPayloadV1`, and a dedicated `build_ability_check_resolved_v1(...)` builder that receives Event ID/timestamp, derives envelope correlation/scope/actor from the Command, uses `caused_by=None`, derives payload from the single resolver outcome, and performs no clock, ID, or persistence work. EventStore remains deferred and remains the future authoritative durable allocator of Event sequence/ID.
- **Consequences:** The immediate typed outcome and durable Event payload may both represent the same roll without a third generic copy. The first read-only handler has a deterministic metadata seam while making no durability claim and without prematurely implementing EventStore. This decision supersedes only DEC-0014's exact `ResolutionResult[T]` field list containing `rolls` and clarifies DEC-0014's statement that Application creates Event metadata; all other DEC-0014 provisions remain historical and authoritative where not superseded.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§2.2, 3.5, 3.10; `CLAUDE.md`; `docs/ROADMAP.md`; `docs/DECISIONS.md`.

## DEC-0020 — Runtime validation is split by invariant ownership

- **Date:** 2026-08-26
- **Status:** Accepted
- **Context:** Strict State/Event deserializers and several Domain Value Objects already validate runtime data, but the canon did not state a general ownership rule. That gap could be read either as requiring transport-shape checks in every dataclass or as allowing Domain constructors to accept semantically impossible objects whenever a serializer was strict.
- **Decision:** Untrusted boundaries strictly validate required/unknown fields, exact runtime types, schema/version, applicable formats, and references when dereferenced, without silent coercion unless a concrete boundary contract permits it. Domain Value Objects own their intrinsic invariants; State and Definitions own semantic in-memory invariants but do not duplicate JSON/document-shape validation. Validation is added for a concrete invariant, not for symmetry with adjacent dataclasses. Typed Domain constructors do not coerce strings, collections, or enums; normalization belongs to boundary mappers/loaders.
- **Consequences:** Boundary and Domain validation remain complementary rather than interchangeable. Existing serializers and intrinsic Value Object checks fit the policy. This decision does not claim that every existing State/Definition invariant is already enforced and does not trigger a mass `__post_init__` retrofit; missing concrete invariants remain work for their owning slices.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §12.25; `CLAUDE.md`; `docs/DECISIONS.md`.

## DEC-0021 — Proficiency begins as a pure character-level rule

- **Date:** 2026-08-26
- **Status:** Accepted
- **Context:** Phase 2 reached Proficiency, while the intentionally minimal Phase 1 State contracts contain neither character level nor proficiency membership and the implemented Ability Check remains a raw `d20 + ability modifier` vertical slice.
- **Decision:** The first Proficiency slice is the pure Domain rule `character_proficiency_bonus(level)`. It accepts exact integer total character levels `1..20` and returns the canonical bonus progression `+2..+6`. The derived bonus is not stored in `CreatureState`; State snapshot schema and Ability Check remain unchanged. Monster proficiency by Challenge Rating is outside this function. Authoritative character-level representation and proficiency membership for skills, saving throws, attacks, tools, and other mechanics remain deferred, as do Expertise, half/double proficiency and stacking rules.
- **Consequences:** The character proficiency progression can be tested independently without prematurely introducing a Character progression model or coupling Proficiency to Skills, Saving Throws, persistence, or the existing raw Ability Check slice. The overall Roadmap item remains incomplete until the concrete proficiency mechanics are implemented.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §3.11; `CLAUDE.md`; `src/dnd_engine/domain/rules/proficiency.py`; `tests/domain/test_proficiency.py`.

## DEC-0022 — Canonical d20 selection semantics

- **Date:** 2026-08-26
- **Status:** Accepted
- **Context:** `DiceRoll.total = sum(rolls)` correctly represents one dice expression but cannot represent effective d20 selection for advantage/disadvantage. The existing Ability Check used `DiceRoll.total`, so a shared d20 primitive was required before Saving Throws and Attack Rolls without changing the Phase 1 dice-expression contract.
- **Decision:** Add the closed `RollMode` enum and immutable `D20Roll(mode, rolls, selected)`, plus the concrete `resolve_d20_roll()` mechanic. NORMAL performs one independent `dice.roll("1d20")`; ADVANTAGE and DISADVANTAGE perform two independent `dice.roll("1d20")` calls and select the maximum or minimum respectively. `dice.roll("2d20")` is not used, and `DiceRoll` remains unchanged. Ability Check accepts a keyword-only effective `roll_mode` defaulting to NORMAL; roll mode is authoritative rule output, not Command, API, or AI input, and Ability Check total uses `D20Roll.selected`. `AbilityCheckResolved` V2 records mode, ordered raw rolls, and selected value and is the current writer. V1 remains an immutable legacy NORMAL-only schema and rejects advantage/disadvantage rather than losing a raw roll.
- **Consequences:** Future Saving Throws and Attack Rolls can reuse the same concrete d20 primitive while advantage sources and Conditions remain deferred. No generic modifier/check framework is introduced, `DiceRoll.total` retains its dice-expression meaning, and existing Event history is not rewritten.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.10, 3.12, 12.10; `CLAUDE.md`; `src/dnd_engine/domain/value_objects/d20.py`; `src/dnd_engine/domain/rules/d20.py`; `src/dnd_engine/domain/rules/ability_check.py`; `src/dnd_engine/domain/events/ability_check.py`; `src/dnd_engine/application/handlers/ability_check.py`.

## DEC-0023 — Character proficiency prerequisites live in dedicated CharacterState

- **Date:** 2026-08-26
- **Status:** Accepted
- **Context:** `CreatureState` is the universal runtime projection for creature instances, while character proficiency bonus is already derived from total character level. Future Saving Throws require authoritative current character level and effective Saving Throw proficiency membership, but those character-only facts must not pollute universal `CreatureState`. The strict State snapshot V1 schema contains no character-specific collection.
- **Decision:** Add mutable `CharacterState` with exactly `id`, `total_level`, and `saving_throw_proficiencies`. Its ID identifies the same runtime character entity and must match an existing `CreatureState.id` in the same `StateSnapshot`; the two projections use composition, not inheritance. `total_level` is an exact integer in `1..20`, and Saving Throw membership is an actual `frozenset[Ability]` with no fixed count. Proficiency bonus remains derived and is not stored. Skill and other proficiency categories, proficiency provenance, class progression, and monster proficiency remain deferred. The current State schema becomes V2 with exact `campaign`, `creatures`, and `characters` state fields. The writer emits only V2; the reader accepts exact V2 and exact legacy V1, mapping V1 to `characters=()` without invented defaults. This supersedes only DEC-0013's current-schema V1 provisions and DEC-0021's deferral of authoritative character level and Saving Throw membership.
- **Consequences:** A future Saving Throw slice can obtain authoritative character inputs without a class system, Skill system, generic proficiency framework, or changes to `CreatureState`. Legacy V1 snapshots remain readable but acquire no inferred progression data; any subsequent save materializes V2. `CharacterState` remains under the existing Creature Domain owner and introduces no mutation use case, Event, or new State Owner in this slice.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.2.3, 3.2.4, 3.11, 10.13, 12.9, 12.12, 12.13; `CLAUDE.md`; `src/dnd_engine/domain/state/character.py`; `src/dnd_engine/domain/state/snapshot.py`; `src/dnd_engine/infrastructure/persistence/json/state_serializer.py`.

## DEC-0024 — First Saving Throw vertical slice is character-specific

- **Date:** 2026-08-26
- **Status:** Accepted
- **Context:** The shared d20 primitive and character proficiency-bonus rule already existed, and `CharacterState` now held authoritative total level and effective Saving Throw proficiency membership. Saving Throw was the second concrete consumer of `ability_modifier`, but no universal monster/Creature proficiency source existed and the first complete Saving Throw flow still needed an explicit Command, resolver, result, Event, and Application handler boundary.
- **Decision:** Command and Event naming remain mechanic-level `SavingThrowCommand` / `SavingThrowResolved`, while the current concrete resolver is `resolve_character_saving_throw(...)` and receives matching `CreatureState` plus `CharacterState` projections. The pure `ability_modifier()` rule moves to shared `domain.rules.ability`; `domain.rules.ability_check` imports and re-exports that same function for compatibility. Saving Throw proficiency is applied only when the selected Ability belongs to `CharacterState.saving_throw_proficiencies`, and its bonus derives from `CharacterState.total_level`. The keyword-only `roll_mode` defaults to NORMAL and is not Command input. `SavingThrowResult` and `SavingThrowResolved` V1 record ability and proficiency contributions separately. The handler is read-only: it emits one Event after successful resolution and never saves State. Missing Creature is `ENTITY_NOT_FOUND`; an existing Creature without matching `CharacterState` is `INVALID_STATE`. Monster Saving Throws remain deferred.
- **Consequences:** Character Saving Throws can be resolved and audited end-to-end without storing derived proficiency bonus, inventing defaults for legacy snapshots, changing the State schema, or introducing a generic modifier/check framework. Gameplay failure remains successful command processing. Ability Check behavior and its public modifier import path remain compatible. The Roadmap Saving Throws item stays incomplete until at least the monster path and its authoritative proficiency source exist.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.7, 3.11–3.13, 12.10; `CLAUDE.md`; `src/dnd_engine/domain/rules/ability.py`; `src/dnd_engine/domain/commands/saving_throw.py`; `src/dnd_engine/domain/rules/saving_throw.py`; `src/dnd_engine/domain/events/saving_throw.py`; `src/dnd_engine/application/handlers/saving_throw.py`.

## DEC-0025 — Skill identity and proficiency membership foundation

- **Date:** 2026-08-27
- **Status:** Accepted
- **Context:** `CharacterState` already held authoritative character level and Saving Throw proficiency membership, but the next Skills slice required canonical Skill identity and persisted effective Skill proficiency membership before introducing any Skill Check flow. Current State schema V2 could not represent that membership, while legacy V1/V2 snapshots had to remain readable without retroactively changing their wire contracts.
- **Decision:** `Skill` is a closed Domain `StrEnum` with exactly 18 canonical lowercase snake-case values. It represents identity only and does not encode a fixed associated Ability, so future rules can support alternative ability checks such as Strength (Intimidation). Extend `CharacterState` with an explicit required `skill_proficiencies: frozenset[Skill]` containing effective current membership; derived proficiency bonus remains unstored. Advance the current State schema to exact V3 by adding required `skillProficiencies` to each Character entry. The writer emits only V3 with deterministic Skill ordering. The reader retains exact V1 and V2 support: V1 maps to `characters=()`, while V2 Character entries map to `skill_proficiencies=frozenset()`. Expertise, half proficiency, Skill Check Command/resolver/Event/handler, and a generic proficiency framework remain deferred. This supersedes only DEC-0023's current-schema V2 provisions and its deferral of Skill proficiency membership.
- **Consequences:** Current snapshots persist authoritative Skill membership without storing modifiers or coupling Skill identity to Ability. Legacy snapshots remain readable and materialize as V3 on the next save. This foundation does not complete the broad Skills or Proficiency Roadmap items and introduces no Skill Check behavior, State Owner, Event, handler, framework, or dependency.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§1.2.2, 3.2.4, 3.11, 12.9, 12.12–12.13; `CLAUDE.md`; `src/dnd_engine/domain/value_objects/skill.py`; `src/dnd_engine/domain/state/character.py`; `src/dnd_engine/infrastructure/persistence/json/state_serializer.py`.

## DEC-0026 — Character Skill Check keeps Skill and Ability explicit

- **Date:** 2026-08-27
- **Status:** Accepted
- **Context:** Canonical Skill identity and persisted character Skill proficiency membership already existed, while Ability Check, Character Saving Throw, shared d20 selection, ability modifiers, character proficiency progression, Event metadata, and read-only handler boundaries established the concrete implementation patterns. A Skill Check still required an explicit contract for alternative Ability use without turning Skill identity into a fixed Skill-to-Ability mapping or introducing a generic check framework after only three consumers.
- **Decision:** `SkillCheckCommand` carries `skill`, `ability`, and `dc` explicitly. The character-specific `resolve_character_skill_check(...)` receives matching `CreatureState` and `CharacterState` projections: explicit Ability selects the score/modifier, while explicit Skill selects membership in `CharacterState.skill_proficiencies`; Strength (Intimidation) is therefore valid without normalization to Charisma. Character proficiency bonus derives from `total_level`, and the resolver delegates effective NORMAL/ADVANTAGE/DISADVANTAGE selection to `resolve_d20_roll()`. `SkillCheckResult` and `SkillCheckResolved` V1 preserve both Skill and actual Ability plus separate ability/proficiency contributions. The handler follows existing lookup/error ordering and remains read-only: it never saves State. Expertise, half proficiency, monster Skill Checks, default association adjudication, and generic check/orchestration abstractions remain deferred.
- **Consequences:** Character Skill Checks can be resolved and audited end-to-end from persisted V3 Skill membership while preserving alternative Ability semantics and existing Ability Check/Saving Throw contracts. Gameplay failure remains successful command processing with one resolved Event. The broad Skills and Proficiency Roadmap items remain unchecked pending an explicit broader Definition of Done; third-handler duplication may be reviewed separately without changing this slice.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.7, 3.11–3.14, 12.10; `CLAUDE.md`; `README.md`; `src/dnd_engine/domain/commands/skill_check.py`; `src/dnd_engine/domain/rules/skill_check.py`; `src/dnd_engine/domain/events/skill_check.py`; `src/dnd_engine/application/handlers/skill_check.py`.

## DEC-0027 — Third d20 consumer does not justify shared check/orchestration abstraction

- **Date:** 2026-08-27
- **Status:** Accepted
- **Context:** Ability Check, Character Saving Throw, and Character Skill Check are now three concrete read-only d20 vertical slices, so their resolvers, handlers, projection lookups, results, and Event payloads were reviewed for duplication. Three consumers triggered a review, not an automatic abstraction. Similar syntax, dataclass shape, or line count is not sufficient; extraction requires evidence of a shared policy, invariant, or boundary.
- **Decision:** No production abstraction justified yet. The existing shared boundaries remain sufficient: `Ability`, `ability_modifier()`, `RollMode`, `D20Roll`, `resolve_d20_roll()`, `character_proficiency_bonus()`, `DiceEngine`, `GameEvent`, `EventMetadataProvider`, `ResolutionResult`, and `StateStore`. `AbilityCheckCommand`/result/Event/resolver/handler, `SavingThrowCommand`/result/Event/resolver/handler, and `SkillCheckCommand`/result/Event/resolver/handler remain concrete. Saving Throws and Skill Checks use different authoritative membership (`Ability` in `CharacterState.saving_throw_proficiencies` versus `Skill` in `CharacterState.skill_proficiencies`), while Ability Checks use no proficiency; only character-level bonus derivation is shared policy. Do not introduce a generic check Command/result/resolver, base resolver, modifier pipeline, proficiency membership framework, callback/strategy-based handler success-tail helper, or generic `ResolutionResult` success/failure factories for constructor shortening. Repeated actor lookup, two-consumer Character projection identity validation, and `getattr`-based Ability score access remain local implementation details rather than repositories, query services, projection registries, generic lookup/failure helpers, or new Domain helpers. The common `mode`/`rolls`/`selected` Event shape is not a shared versioned wire sub-contract or builder: Event schemas version independently, including legacy/current Ability Check V1/V2 versus Saving Throw and Skill Check V1.
- **Consequences:** Re-evaluate actor/target lookup policy, d20 consumer boundaries, and handler orchestration after a concrete Attack Roll slice, without designing Attack Roll as an Ability Check. Target, attack-specific sources, AC comparison, weapon/attack semantics, and mechanic-owned natural-1/natural-20 or critical semantics are likely new evidence, not an already fixed Attack Roll contract. Re-evaluate `resolution → Events → State application → persistence` after the first real state-mutating action such as HP, Damage, or Healing, because the current read-only handlers provide no evidence for that orchestration. Revisit a Character projection helper after at least a third concrete character-specific consumer needs both `CreatureState` and `CharacterState`, and revisit proficiency abstraction only when materially new semantics such as Expertise, half proficiency, or monster/non-character sources exist. Test-only repetition in StateStore spies/fakes, scripted/failing Dice doubles, fixed metadata providers, and Campaign/Creature fixtures may be extracted in a separate low-risk cleanup without any production abstraction; dedicated mechanic integration tests remain separate, and no generic testing framework or DSL follows from this review.
- **Affected contracts/files:** Changed: `docs/DECISIONS.md`. Reviewed as context and unchanged: `docs/ARCHITECTURE.md` §§3.5–3.6, 3.10–3.14 and the concrete Ability Check, Character Saving Throw, and Character Skill Check handler/rule/Event implementations.

## DEC-0028 — Armor Class is derived from authoritative sources

- **Date:** 2026-08-27
- **Status:** Accepted
- **Context:** Phase 2 reached AC. The existing `CreatureState` and `CharacterState` intentionally contain no AC or equipment fields, and Equipment already has a separate future owner (§10.6). The first Monster AC consumer will require a real typed Definition dereference for the first time, while the current implemented Phase 1 `MonsterDefinition` (§3.1.1) is a closed minimal contract without `armor_class`. Nothing in the canon yet stated whether effective AC is persisted State or a derived rule result, or where Monster AC implementation sits relative to a future Definition-loading slice.
- **Decision:** Effective Armor Class is a derived Domain rule result, not persisted State, not a Definition, and not a new State Owner; it is not stored as a materialized field on `CreatureState` or `CharacterState`, and no `ArmorClassState`/`ACState` is introduced. Authoritative State/Definition continues to hold only the sources AC is computed from. Creature ability ownership (§10.4) and Equipment armor/shield ownership (§10.6) are unchanged; this decision does not determine Conditions/Effects ownership or composition for AC purposes — their contribution to effective AC remains deferred until the corresponding mechanic is designed, and their existing separate treatment under §10.4 (Creature Domain) and §10.13 (`EffectState`/Effect Engine) is left as-is. The initial Character AC scope is the unarmored formula `10 + Dexterity modifier`, sourced from `CreatureState.ability_scores.dexterity` via the existing `ability_modifier()` rule, without `CharacterState`, proficiency, `DiceEngine`, or a State write-back. The initial Monster AC scope is a future immutable `MonsterDefinition.armor_class` baseline dereferenced through `CreatureState.definition_id`, not derived from Dexterity; `armor_class` is not added to the current implemented §3.1.1 `MonsterDefinition` schema in this slice — it is a G4a prerequisite. G4a is the required pipeline gate before the whole AC IMPLEMENTATION slice — both unarmored Character AC and baseline Monster AC — not only before the Monster AC branch; neither begins before G4a completes. Only Monster AC technically depends on typed Definition access, but that technical asymmetry does not authorize starting Character AC implementation ahead of G4a; it does not establish any required order or parallelism between Character AC and Monster AC once G4a is complete, either — that sequencing is left to a later implementation slice. G4a must establish the minimal typed Definition access boundary (port, typed lookup, `MonsterDefinition.armor_class`, minimal real data, lazy referential validation, `DEFINITION_NOT_FOUND` for missing Definitions, a wrong-type failure policy, packaged ruleset resources, and an installed-wheel test); the exact Python signature and implementation are not designed here. Referential validation stays lazy, validating a reference only when a concrete mechanic dereferences it; no eager/global campaign-graph validation is introduced. AC calculation is a read-only Domain query, not a Command/Event use case: no `ArmorClassCommand`/payload/result/Event/handler is introduced, and AC calculation does not mutate State, call `StateStore.save()`, create a `GameEvent`, or use `EventMetadataProvider`/`DiceEngine`. Attack will consume effective target AC without owning or persisting it; a future Attack Event may record the AC actually used as an audit fact, but the exact Attack Event schema is not fixed here. No generic `ArmorClassProvider`/`ArmorClassStrategy`/`ArmorClassFormula` hierarchy, `DefenseProfile`, or generic modifier/AC pipeline is introduced, consistent with DEC-0027.
- **Consequences:** State keeps holding authoritative inputs rather than a duplicated derived AC value, so no second source of truth is created. Equipment ownership (§10.6) remains unchanged, and Conditions/Effects ownership stays exactly as split across §10.4/§10.13 today — this decision neither resolves nor hardens that split. Monster baseline AC is a Definition fact, not a Dexterity-derived value. G4a blocks the start of AC IMPLEMENTATION as a whole, including unarmored Character AC, even though only the Monster AC branch technically needs typed Definition access; this decision fixes no relative order or parallelism between the two branches after G4a completes. Armor, shields, alternative AC formulas, runtime AC modifiers, and Attack's concrete contract remain deferred. This decision documents an approved design; it implements no Python code, adds no `MonsterDefinition.armor_class` field, and does not mark the Roadmap AC item complete.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §3.15; `docs/DECISIONS.md`; `docs/DEVELOPMENT_LOG.md`.

## DEC-0029 — Definition access and packaged SRD 5.1 ruleset boundary (G4a)

- **Date:** 2026-08-27
- **Status:** Accepted
- **Context:** DEC-0028 approved the Armor Class design and fixed G4a as the mandatory prerequisite gate before any AC IMPLEMENTATION, requiring a typed Definition access boundary, `MonsterDefinition.armor_class`, lazy referential validation, missing/wrong-type failure policy, packaged ruleset resources, and an installed-wheel test, without designing their exact shape. Separately, `ruleset_version="5.2.1"` had been used throughout the canon and test fixtures as the current example value for `dnd_5e` without ever being tied to a specific SRD edition, and the top-level `rules/dnd_5e/` tree was pure `.gitkeep` scaffold outside the installable `src/` package tree — not a reliable installed-wheel resource location, and a second potential location once real data existed.
- **Decision:** `dnd_5e` in this project means classic Dungeons & Dragons 5th Edition (2014 rules), i.e. System Reference Document 5.1 ("SRD 5.1") — not SRD 5.2.x / the 2024 ("5.5e") revision. The canonical `ruleset_version` for `dnd_5e` is `"5.1"`; the ambiguous `"5.2.1"` example value is replaced everywhere it was used as the current `dnd_5e` value (`docs/ARCHITECTURE.md` §§4.6–4.7, 12.9; test fixtures), and no new `Ruleset` Value Object is introduced — `ruleset_id`/`ruleset_version` remain the existing plain `str` fields on `CampaignState` (§3.2.2). `MonsterDefinition` (§3.1.1) gains one field, `armor_class: int`, an immutable baseline stat-block fact rejecting `bool` and any other non-`int`; no numeric range beyond that is added, and no other monster fields are added. A new Domain port `DefinitionSource` (`src/dnd_engine/domain/services/definitions.py`, §3.16) declares `get_definition(*, ruleset_id: str, ruleset_version: str, definition_id: str, expected_type: type[TDefinition]) -> TDefinition`, following the same port-in-Domain pattern as `StateStore`; it takes only the two ruleset identity strings, never the full `CampaignState`, never returns `None`, and is sync-only. Two semantic exceptions live beside it: `DefinitionNotFoundError` (ruleset/version resolved, `definition_id` absent — future Application mapping is the existing `ErrorCode.DEFINITION_NOT_FOUND`, no new code) and `DefinitionTypeMismatchError` (Definition exists but fails `isinstance(definition, expected_type)` — future Application mapping is `ErrorCode.INVALID_STATE` with `field="definition_id"`). No concrete Application consumer exists yet, so no generic exception→`EngineError` mapping function is added; the mapping is fixed in the canon (§3.16) for the future AC consumer to follow. Referential validation stays lazy: `StateStore`/`StateSerializer`/`StateSnapshot` never dereference `definition_id`, proven by a regression test asserting a snapshot with a nonexistent `definition_id` still loads. Packaged ruleset resources move into the installable package at `src/dnd_engine/resources/rulesets/<ruleset_id>/<ruleset_version>/definitions/<definition_id>.json` (§12.26), read via `importlib.resources` by a stateless production adapter `PackagedDefinitionSource` (`src/dnd_engine/infrastructure/definitions/packaged.py`) that accepts an optional `resources_root` override for isolated tests without a second production dataset. Each resource JSON carries an explicit `type` discriminator (`"monster"` / `"item"` / `"weapon"`) that is not a Domain field; the adapter decodes the actual type via a small deterministic `if`/`elif`/`else`, then validates `isinstance(decoded, expected_type)`, so an actual `WeaponDefinition` satisfies an expected `ItemDefinition`. The loader is a strict untrusted boundary (§12.25): malformed JSON, non-object root, missing/unknown fields, wrong primitive types (including `bool`-for-`int` and stringified numbers), malformed `abilityScores`, unknown `type`, invalid Domain values, and a payload `id` that does not match the requested `definition_id` all raise a distinct Infrastructure `InvalidPackagedDefinitionError` — never silently coerced, and never collapsed into `DefinitionNotFoundError`. `pyproject.toml` gained `[tool.setuptools.package-data]` for `dnd_engine.resources` so the wheel actually contains the JSON/`NOTICE.md`; no production dependency was added. The single production dataset is one SRD 5.1 `goblin` `MonsterDefinition` (id, version, name, ability scores, armor class only), verified against the official SRD 5.1 Goblin stat block before packaging, with a `NOTICE.md` recording SRD 5.1 / CC-BY-4.0 attribution (the official Creative Commons SRD 5.1 release, not the OGL 1.0a PDF); no other monster and no weapon production dataset is added (G4b remains untouched). The obsolete top-level `rules/dnd_5e/` scaffold (`.gitkeep` placeholders only) is removed so exactly one authoritative packaged Definition dataset exists; `AGENTS.md`, `CLAUDE.md`, and `README.md` are updated to stop naming `rules/dnd_5e/` as the canonical Definition location. `tests/packaging/test_installed_wheel_definitions.py` builds a real wheel from an isolated copy of the source tree (so setuptools' `build/` staging never lands in the repository), installs it into a fresh venv (never `pip install -e`), and runs a child process outside the checkout with `PYTHONPATH` cleared, proving both the successful `goblin` lookup and `DefinitionNotFoundError` for a missing id resolve from the installed wheel alone. No `DefinitionRegistry`, plugin/entry-point system, service locator, DI container, cache, or generic Definition serialization framework is introduced, consistent with DEC-0027 and the deferred-abstractions list (§3.6). Following review, `ruleset_id`/`ruleset_version`/`definition_id` are validated as one canonical resource path segment each (§4.1/§4.6 ID contract; no `/`, `\`, or `.`/`..` path semantics) before any value is joined into a resource path, and a non-canonical value is `DefinitionNotFoundError` rather than a new exception type; a missing/broken top-level packaged `rulesets/` resource root is `InvalidPackagedDefinitionError`, distinct from an ordinary missing/unsupported ruleset-scope lookup, which remains `DefinitionNotFoundError`.
- **Consequences:** AC IMPLEMENTATION (unarmored Character AC, baseline Monster AC, Attack's use of effective AC) remains not implemented; this decision only clears the G4a gate DEC-0028 set. `docs/ROADMAP.md`'s AC and Attack rolls items remain unchecked. Any future Definition category (spells, classes, weapons for G4b, etc.) has one obvious location to add packaged JSON to, with no second candidate directory. Historical entries referencing `"5.2.1"` as a past state are not rewritten; only current canonical/example usages changed.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.1.1, 3.15, 3.16, 4.6–4.7, 12.9, 12.26; `docs/DECISIONS.md`; `docs/DEVELOPMENT_LOG.md`; `AGENTS.md`; `CLAUDE.md`; `README.md`; `pyproject.toml`; `src/dnd_engine/domain/definitions/monster.py`; `src/dnd_engine/domain/services/definitions.py`; `src/dnd_engine/infrastructure/definitions/packaged.py`; `src/dnd_engine/resources/rulesets/dnd_5e/5.1/definitions/goblin.json`; `src/dnd_engine/resources/rulesets/dnd_5e/5.1/NOTICE.md`; removed `rules/dnd_5e/`.

## DEC-0030 — Shared Domain `NdM` parser for Dice Engine and WeaponDefinition (G4b)

- **Date:** 2026-08-27
- **Status:** Accepted
- **Context:** `WeaponDefinition.damage_dice: str` was already part of the Phase 1 Definition contract (DEC-0008), with DEC-0009 subsequently fixing its `DamageType` contract; neither decision enforced `NdM` notation on `damage_dice` itself. DEC-0011 kept the strict `NdM` parser private to the `PythonDiceEngine` Infrastructure adapter because parsing had exactly one enforcing consumer at that point. G4a (DEC-0029) later added packaged Definition access and `_decode_weapon()`; that boundary validated JSON shape/primitive types and `DamageType`, but still did not enforce `NdM` notation on `damage_dice`. G4b introduces the second enforcing semantic consumer of the same accepted-language rule, `WeaponDefinition.damage_dice`, plus the first production weapon Definition (`dagger`). Without one shared invariant, a `WeaponDefinition` could be constructed, directly or via the packaged loader, with a `damage_dice` value that the runtime `DiceEngine` would later reject — an avoidable Definition/Engine mismatch that DEC-0011's Infrastructure-private parser could not prevent.
- **Decision:** The strict minimal `NdM` parser/extractor becomes one pure shared Domain primitive, `parse_ndm(expression: str) -> tuple[int, int]` in `src/dnd_engine/domain/dice.py`. It accepts only exact `str` (`TypeError("expression must be a str")` otherwise), enforces grammar `[1-9][0-9]*d[1-9][0-9]*` (`ValueError("invalid dice expression")` for malformed syntax) and `sides >= 2` (`ValueError("dice must have at least two sides")`) — identical observable behavior to the parser it replaces. `PythonDiceEngine.roll()` and `WeaponDefinition.__post_init__` both call this one function; `PythonDiceEngine`'s own regex/parsing code is removed, not duplicated, and RNG call count/order/`DiceRoll.total` computation are unchanged. `parse_ndm` imports nothing beyond stdlib `re`: it does not know about `random.Random` or `DiceRoll`, so RNG execution remains an Infrastructure-only concern and Domain still does not depend on Infrastructure. `damage_dice` remains plain `str` — no `(count, sides)` tuple, `DiceExpression` Value Object, AST, or generic modifier machinery is introduced, and no `WeaponDefinition` field is added or renamed. One production weapon Definition, `dagger` (SRD 5.1: `1d4` piercing; properties `finesse`/`light`/`thrown`), is packaged as the concrete second consumer proof, decoded by the existing, unmodified `_decode_weapon()` dispatch in `PackagedDefinitionSource`; a malformed packaged `damageDice` (e.g. `"1D4"`) surfaces through the existing `TypeError`/`ValueError` → `InvalidPackagedDefinitionError` wrapping already used for other Domain construction failures, with no new exception type or decoder framework. The existing installed-wheel proof (`tests/packaging/test_installed_wheel_definitions.py`) is extended to also resolve `dagger` as `WeaponDefinition` from the installed wheel, proving `dagger.json` ships inside the package rather than only existing in the checkout.
- **Consequences:** A `WeaponDefinition` can no longer be constructed, directly or through the packaged loader, with dice notation that the runtime `DiceEngine` would later reject; the two consumers share one accepted-language rule and cannot silently drift apart. DEC-0011's `DiceEngine`/`DiceRoll`/RNG-injection contract is otherwise unaffected and remains authoritative; this decision narrowly supersedes only its "parsing remains private to the Infrastructure implementation" clause, now that Domain owns a second, non-RNG consumer of the same grammar. Attack rolls, damage resolution, HP, weapon attack bonus, range/ammunition/versatile/two-handed mechanics, a generic modifier pipeline, and any generic dice DSL/registry remain deferred; `docs/ROADMAP.md`'s Attack rolls item stays unchecked.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§1.7.1, 3.1.1, 12.26; `docs/DECISIONS.md`; `docs/DEVELOPMENT_LOG.md`; `CLAUDE.md`; `src/dnd_engine/domain/dice.py` (new); `src/dnd_engine/domain/definitions/weapon.py`; `src/dnd_engine/infrastructure/random/dice.py`; `src/dnd_engine/resources/rulesets/dnd_5e/5.1/definitions/dagger.json` (new); `src/dnd_engine/resources/rulesets/dnd_5e/5.1/NOTICE.md`; `tests/domain/test_dice_parser.py` (new); `tests/domain/test_weapon_definition.py`; `tests/infrastructure/test_packaged_definition_source.py`; `tests/packaging/test_installed_wheel_definitions.py`.

## DEC-0031 — First Character unarmed Attack Roll → Monster slice stays concrete

- **Date:** 2026-08-28
- **Status:** Accepted
- **Context:** The AC/G4a/G4b prerequisites now exist: baseline Monster AC is an immutable `MonsterDefinition.armor_class` fact, Campaign ruleset identity plus `CreatureState.definition_id` can be resolved through typed `DefinitionSource`, and weapon damage dice have a strict shared invariant. Shared `D20Roll`/`resolve_d20_roll()`, `ability_modifier()`, and `character_proficiency_bonus()` rules also exist. DEC-0027 explicitly scheduled the first concrete Attack Roll as the next abstraction re-evaluation checkpoint after Ability Check, Character Saving Throw, and Character Skill Check, while warning that target, AC comparison, natural-1/natural-20, critical, and attack-source semantics were new evidence rather than proof of a generic check abstraction. Broader weapon/equipment, weapon-proficiency, Monster attack, spell attack, targeting, damage, and HP inputs do not yet exist.
- **Decision:** Implement and canonize exactly one narrow read-only path: `Character unarmed attack → Monster target`. Intent remains `AttackCommand`, whose payload contains only `target_id`; caller-supplied attack bonus, proficiency, AC, weapon, or ability selection is forbidden. `AttackCommand` contains no roll mode, and player/API/AI intent does not currently provide one; `roll_mode` remains a valid keyword-only effective input to `resolve_character_unarmed_attack()`, while production `AttackHandler` passes no override and therefore uses the default `RollMode.NORMAL`. `AttackHandler` loads matching actor `CreatureState` plus `CharacterState`, loads the target `CreatureState`, resolves its `definition_id` under Campaign `ruleset_id`/`ruleset_version` through `DefinitionSource(expected_type=MonsterDefinition)`, and consumes `MonsterDefinition.armor_class` without owning or persisting AC. `resolve_character_unarmed_attack()` uses Strength, `ability_modifier(actor Strength)`, and the derived `character_proficiency_bonus(CharacterState.total_level)`; this Character unarmed path is always proficient. It reuses `D20Roll` selection, while Attack owns the policy on `D20Roll.selected`: natural 1 is an automatic miss, natural 20 is an automatic hit with `critical_hit=true`, and every other result hits when total is at least target AC. Successful processing produces `AttackResult`, exactly one read-only `AttackResolved` V1 Event recording the actual target AC used, and `ResolutionResult[AttackResult]`; hit, miss, and critical are fields of the same Event, not separate `AttackHit`/`AttackMissed`/`CriticalHit` Events. No State save, Event application/persistence, damage, critical damage, or HP mutation occurs. The post-Attack abstraction verdict is **KEEP CONCRETE**: reject a generic d20/check resolver, Character projection wrapper, actor/target lookup helper, generic Definition-exception mapper, handler success-tail helper, proficiency framework, and `ModifierPipeline`. In accordance with DEC-0027, consumer count is a review trigger rather than an abstraction rule.
- **Consequences:** The repository has its first end-to-end Attack Roll resolution and audit Event, but not a combat or weapon system and not a State-mutating action. Existing Ability Check, Character Saving Throw, and Character Skill Check contracts remain unchanged, and no generic framework is introduced. Dagger/weapon attacks, equipment/inventory, weapon proficiency, Finesse, range/reach/ammunition, Character targets, Monster attacks, spell attacks, broader targeting/visibility/cover, damage, critical damage, and HP remain deferred. Damage/HP is the first future checkpoint that can provide real evidence for `resolution → Events → State application → persistence` orchestration. The broad Roadmap `[ ] Attack rolls` item remains incomplete because this decision covers only Character unarmed → Monster, not the broader mechanic Definition of Done.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.3, 3.7–3.8, 3.12, 3.15–3.17, 8–9, 11; `docs/DECISIONS.md`; `docs/DEVELOPMENT_LOG.md`; `CLAUDE.md`; `README.md`; implemented source in `src/dnd_engine/domain/commands/attack.py`, `src/dnd_engine/domain/rules/attack.py`, `src/dnd_engine/domain/events/attack.py`, and `src/dnd_engine/application/handlers/attack.py`; corresponding Domain, Application, and integration tests.

## DEC-0032 — First authoritative State mutation uses snapshot-authoritative Event-driven replacement

- **Date:** 2026-08-28
- **Status:** Accepted
- **Context:** §3.8 Atomicity deferred exact ordering, mutation-scope, and Event-application semantics to "a separate State Mutation Foundation decision" once the Ability Check (§3.10), Character Saving Throw (§3.13), Character Skill Check (§3.14), and Character unarmed Attack Roll (§3.17, DEC-0031) read-only slices existed. DEC-0031 explicitly named Damage/HP as the first checkpoint that can supply real evidence for `resolution → Events → State application → persistence` orchestration. No production gameplay handler calls `StateStore.save()`, `StateStore` remains a `load()`/`save()`-only Domain port, `StateSnapshot` stays a frozen container of mutable `CreatureState`/`CharacterState` objects, and `ResolutionResult` stays `success`/`command_id`/`outcome`/`events`/`errors` with no `state_changes` field.
- **Decision:** §3.18 State Mutation Foundation (G5) fixes, ahead of the first concrete Damage → HP implementation, the canonical contract that every future authoritative state-mutating Command must follow. Gameplay rule resolution, Event construction, and State application stay three separate responsibilities: a resolver determines the concrete gameplay outcome only and neither constructs Events nor receives `EventMetadataProvider`; Application then constructs the complete ordered Event batch from that outcome together with authoritative Event metadata (§2.2, §3.10) — the same division the implemented Ability Check, Character Saving Throw, Character Skill Check, and Character unarmed Attack Roll handlers already use; and a State Owner-specific deterministic application step projects an already-resolved Event onto a replacement State object without recomputing the gameplay decision, rolling dice, loading Definitions, calling the AI layer, performing persistence I/O, reading the clock, or allocating IDs. Every State object obtained through `StateStore.load()` is read-only authoritative input for the current transaction; authoritative mutation is expressed through replacement/copy-on-write construction of a new State object, a new collection, and a new `StateSnapshot`, never through in-place mutation of the loaded graph, and `deepcopy()`, a `WorkingState` wrapper, and making State dataclasses frozen are explicitly not part of this contract. Each concrete State Owner transition gets its own explicit mutation scope, documented and reviewed alongside that transition's concrete Event contract rather than through a mandatory dedicated Decision Log entry; for the future Damage → HP consumer only `CreatureState.current_hp` may change, while `id`, `definition_id`, `ability_scores`, and `max_hp` are preserved, without declaring those fields globally immutable for other future transitions. Canonical mutating ordering is outcome → Events → replacement State → invariants → replacement `StateSnapshot` → `StateStore.save()` → successful `ResolutionResult`; a successful mutating result is never observable before `save()` returns, and the "return success, save later" model is forbidden. If `save()` fails, the loaded State stays unchanged, no successful `ResolutionResult` is returned, in-memory Events are not treated as durable history, the Infrastructure failure is not auto-converted into a gameplay `EngineError`, and no rollback/ID-reuse mechanism is introduced — ID gaps are accepted. For this MVP the persisted `StateSnapshot` is the authoritative persisted representation of campaign State; current Events are immutable Domain facts, not durable runtime history, and `FilesystemStateStore`'s atomic single-file replacement is not described as fsync/power-loss/crash-durable. EventStore, durable Event ID/sequence allocation, and serialized-Event type/version dispatch for reading persisted Events all remain deferred, because pairing `EventStore.append(events)` with `StateStore.save(snapshot)` without a shared transaction design would create an inconsistency window that this MVP does not attempt to close. `ResolutionResult.state_changes` is not introduced: the resolved Event stays the single authoritative representation of a transition. `UnitOfWork`, `TransactionManager`, `WorkingState`, `MutationContext`, `StateChange`, `EventApplierRegistry`, a generic reducer, a generic State Owner repository, and a generic transaction coordinator all remain deferred alongside the existing §3.6 list. The MVP atomicity guarantee is limited to a single-snapshot, single-writer boundary: the loaded State is not mutated, the replacement is built in isolation, persisted State does not change before `save()`, exactly one `save()` call persists the replacement, and a successful result follows only a successful save — with no atomic EventStore+StateStore transaction, no distributed/multi-store transaction, no concurrency control, no optimistic locking/revision/CAS, no replay, no rollback after a successful save, no exactly-once execution, no retry deduplication, and no crash/power-loss recovery guaranteed.
- **Consequences:** The repository has a canonical target for the first Damage → HP implementation slice without implementing Damage, HP mutation, an Event applier, or any generic orchestration framework. §3.8 Atomicity is clarified to point at this decision instead of an unnamed future one. No Python contract, test, `StateStore` signature, `StateSnapshot` schema, or `ResolutionResult` field changes. The first Damage → HP slice remains the evidence checkpoint that can justify revisiting any of the deferred abstractions listed here.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.8, 3.18; `docs/DECISIONS.md`; `docs/ROADMAP.md`; `CLAUDE.md`; `docs/DEVELOPMENT_LOG.md`.

## DEC-0033 — First concrete Damage → HP mutation slice (G6A) stays concrete

- **Date:** 2026-08-28
- **Status:** Accepted
- **Context:** DEC-0032/§3.18 fixed the canonical contract a future authoritative state-mutating Command must follow, and named Damage/HP as the first checkpoint able to supply real evidence for `resolution → Events → State application → persistence` orchestration, without implementing it. This decision records the first concrete implementation of that contract: `ApplyDamageCommand`/`ApplyDamagePayload`, the pure `resolve_damage`/`DamageResult` resolver, the `DamageApplied` V1 `GameEvent`, the concrete `apply_damage_applied_v1` Creature applier, and the `DamageHandler` Application orchestrator that calls `StateStore.save()` for the first time in any production gameplay code path (§3.19).
- **Decision:** Direct, already-resolved Damage was chosen as the first mutation consumer — not Attack → Damage — because §3.18's obligations are about the mutation mechanics (Command → Event → State replacement → persistence), and coupling the first mutation slice to Attack resolution would additionally require weapon damage dice, `DamageType` mechanics, and critical-damage rules that are separate, larger, evidence-driven decisions; `ApplyDamageCommand` therefore takes an already-resolved `amount: int >= 1` and is not constructed from, or aware of, `AttackResult`. `DamageApplied` V1 stores both `previousHp` and `newHp` (not `newHp` alone) so the Event is a complete, self-contained record of the transition and so State application never needs to recompute the `max(0, previous_hp - amount)` clamp — that formula invariant is owned exclusively by `DamageResult`, and duplicating it in the Event builder or the applier was rejected as redundant, competing rule ownership. Event application (`apply_damage_applied_v1`) executes on the `CreatureState` Owner boundary named in §10.4/§10.15: it is a State Owner-specific deterministic projection, not a generic reducer, and it only decides whether the Event is applicable to the supplied Creature (matching `targetId` and `previousHp`) before returning a `dataclasses.replace(...)` copy. Application (`DamageHandler`), not the applier, constructs the replacement `StateSnapshot` — substituting the replaced Creature into a new `creatures` tuple and reusing the unchanged `CampaignState`/`characters` — because snapshot construction is an orchestration concern spanning multiple loaded objects, while the applier's concern is limited to one Creature. A `0 → 0` transition is allowed to succeed, because no death/unconscious semantics exist yet and rejecting harmless no-op Damage at zero HP would be inventing gameplay behaviour ahead of a real consumer; but no-op-replay detection is explicitly not promised, because `previousHp`-based staleness rejection only works when the transition actually changes a value — distinguishing a genuine repeated `0 → 0` Damage instance from an idempotent retry would need deferred revision/idempotency/replay state that §3.18's "Exact MVP atomicity boundary" already excludes, and inventing it here for one edge case was rejected as scope creep. `EventStore`, `state_changes`, `UnitOfWork`, and a generic Event-applier registry all remain deferred per the existing §3.6/§3.18 lists: the post-G6A implementation review recorded here and in §3.19 found that the current concrete Damage consumer does not provide sufficient evidence for any of them, nor for a shared HP mutation primitive, so `KEEP CONCRETE` remains the verdict — DEC-0032 deferred these abstractions until concrete evidence existed, it did not itself pre-decide that one implemented consumer would necessarily be insufficient. Broad Roadmap `HP`/`Damage` are not marked complete, because this slice deliberately excludes weapon rolls, `DamageType` mechanics, resistance/immunity/vulnerability, temporary HP, death/unconscious, healing, conditions, and Attack → Damage orchestration (§3.19 "Explicit exclusions"). Healing is named as the next meaningful evidence checkpoint because it is the first expected second HP-mutating use case with materially related but different semantics — moving `current_hp` the opposite direction, clamped at `max_hp` instead of `0`; when Healing exists, shared HP mutation behaviour should be re-evaluated based on the actual common responsibilities, invariants, and differences the two implementations demonstrate, not merely because the consumer count reaches two.
- **Consequences:** The repository has its first production State-mutating gameplay path: `ApplyDamageCommand → resolve_damage → DamageApplied V1 → apply_damage_applied_v1 → replacement StateSnapshot → StateStore.save()`. §3.18's conceptual guarantees now have one concrete, tested instance (§3.19) rather than remaining purely conceptual. No `ErrorCode`, `StateStore` signature, `StateSnapshot` schema, or `ResolutionResult` field changed. Broad `HP`/`Damage`/`Healing` stay unchecked in `docs/ROADMAP.md`. The deferred-abstraction lists in §3.6/§3.18 are reaffirmed, not revisited.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §3.19; `docs/DECISIONS.md`; `docs/ROADMAP.md`; `README.md`; `CLAUDE.md`; `docs/DEVELOPMENT_LOG.md`.

## DEC-0034 — Minimal Healing → HP mutation slice (G6B) stays concrete

- **Date:** 2026-08-28
- **Status:** Accepted
- **Context:** G6B supplies the second production consumer of the §3.18 authoritative State-mutation lifecycle. Groups 1–3 implemented `ApplyHealingCommand`/`ApplyHealingPayload`, pure `resolve_healing`/`HealingResult`, `HealingApplied` V1, the concrete `apply_healing_applied_v1` Creature applier, and `HealingHandler` with authoritative replacement-snapshot persistence. This provides the evidence checkpoint DEC-0033 requested: Damage and Healing both update `CreatureState.current_hp`, but their actual gameplay decisions and integrity inputs are not identical. The canonical contract and the post-G6B abstraction verdict had not yet been recorded.
- **Decision:** Canonize the implemented G6B slice in §3.20. `ApplyHealingPayload(target_id, amount)` carries an exact positive `int` amount that is already resolved and source-agnostic; it deliberately says nothing about spells, items, features, rests, dice, modifiers, or resource consumption. `HealingResult(target_id, amount, previous_hp, max_hp, new_hp)` exclusively owns `new_hp == min(max_hp, previous_hp + amount)`, while `build_healing_applied_v1` only checks Command/outcome correlation and records the validated result. `HealingApplied` V1 contains exactly `targetId`, `amount`, `previousHp`, `maxHp`, and `newHp`: `amount` preserves the resolved input, the endpoints preserve the actual capped transition, and `maxHp` records the mutable authoritative limit used by the decision. No `appliedAmount` is added because the effective HP gain is derivable as `newHp - previousHp`; storing it separately would duplicate one transition fact and introduce another consistency invariant. The concrete applier validates Event type/version/payload, `targetId`, `previousHp`, and `maxHp`, then projects the supplied `newHp` through `dataclasses.replace`; it does not repeat the Healing formula. Healing from zero HP is valid. Positive Healing at full HP is a successful no-op, and still produces a `HealingApplied` Event, replacement Creature and snapshot, exactly one `StateStore.save()`, and success only after save. Duplicate application of a full-HP no-op is not detected because no revision/idempotency/replay mechanism exists. The explicit Damage/Healing review considered a shared current-HP transition primitive, replacement-Creature helper, snapshot replacement helper, stale-state/integrity helper, generic Event applier, generic mutation handler, `WorkingState`, `UnitOfWork`/`TransactionManager`, `ResolutionResult.state_changes`, and `EventApplierRegistry`/generic reducer. The verdict for every candidate is **KEEP CONCRETE**. Damage owns `max(0, previous_hp - amount)` and correlates target/previous HP; Healing owns `min(max_hp, previous_hp + amount)`, additionally reads authoritative `max_hp`, records `maxHp`, and correlates it during application. The remaining overlap is predominantly duplicated syntax or visible sequencing: the narrow owner-specific replacement candidate would wrap one `dataclasses.replace` call; the snapshot helper would save only a small local tuple/snapshot block while taking ownership of aggregate failure semantics; integrity helpers would need Event-specific decoding/callbacks; and a generic handler/applier would require type parameters, callbacks, registries, or policy hooks that obscure the concrete persistence boundary. There is no third production mutation consumer and no serialized Event/replay dispatcher. Concrete code is therefore cheaper and clearer than the proposed indirection. `EventStore`, durable Event history, `state_changes`, `WorkingState`, `UnitOfWork`, transaction coordination, generic Event dispatch/reduction, State revision/CAS, retry deduplication, and exactly-once execution remain deferred under §§3.6 and 3.18.
- **Consequences:** The repository now has two tested authoritative HP mutation paths under one State Mutation Foundation without merging their gameplay math or introducing a shared mutation framework. Healing changes only `CreatureState.current_hp`; `id`, `definition_id`, `ability_scores`, and `max_hp` remain unchanged. `HealingHandler` retains the G6A actor/target semantics, replacement-snapshot ordering, save-failure propagation, and snapshot-authoritative persistence boundary. `StateStore`, `StateSnapshot`, `ResolutionResult`, serialized State schema/version, and production dependencies do not change. Broad Roadmap `HP`/`Damage`/`Healing` remain unchecked; this decision neither completes them nor makes their completion conditional on any later documentation group.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.18–3.20; `docs/DECISIONS.md`; `CLAUDE.md`; `docs/DEVELOPMENT_LOG.md`; already-implemented G6B production code in `src/dnd_engine/domain/commands/healing.py`, `src/dnd_engine/domain/rules/healing.py`, `src/dnd_engine/domain/events/healing.py`, and `src/dnd_engine/application/handlers/healing.py` plus their tests (no production code changed by this decision slice).

## DEC-0035 — Condition State foundation (G6C1): persisted membership only, State schema V4

- **Date:** 2026-08-28
- **Status:** Accepted
- **Context:** Conditions were named as deferred/future scope throughout the G6A (DEC-0033) and G6B (DEC-0034) Explicit exclusions, and `condition_NNN`/`poisoned` were already reserved in the canonical ID registry (§4.12/§4.13) as demonstrational identifiers with no concrete consumer. G6C begins Conditions work; this first group, G6C1, supplies only the foundational persisted representation — a closed `Condition` identity and `CreatureState.conditions` membership — with no Apply/Remove Command, Event, handler, or gameplay effect (those are separate later G6C groups, matching §3.6's rule against introducing future-phase behaviour ahead of a concrete consumer). `CreatureState`/`CharacterState` and the V1–V3 State schema (§3.2, §12.9) existed unchanged; `StateSerializer`'s Character `skillProficiencies` decoding branch was gated by direct equality against the current `SCHEMA_VERSION` constant rather than an explicit legacy-version comparison, which meant a naive `SCHEMA_VERSION = 4` bump would have silently stopped V3 payloads from decoding their Character projection — a concrete regression, not a hypothetical one, discovered while implementing this slice. Review of the first implementation of this slice found the same trap reintroduced for the new V4 Creature shape itself: the V4 Creature field set and `conditions` decoding were both gated by `schema_version == SCHEMA_VERSION`, so a future `SCHEMA_VERSION` bump would silently misread already-persisted V4 Creature payloads as pre-V4 and reject their `conditions` field as unknown.
- **Decision:** Add a closed, identity-only Domain `StrEnum` `Condition` (`src/dnd_engine/domain/value_objects/condition.py`) with exactly one member, `POISONED = "poisoned"`, following the same pattern as `DamageType`/`Skill`; no other 5e Condition is added ahead of a concrete consumer. `CreatureState` gains `conditions: frozenset[Condition] = frozenset()` (§3.21): intrinsic `__post_init__` validation requires `type(conditions) is frozenset` and every member an actual `Condition`, with no coercion from `list`/`set`/`tuple`/`frozenset[str]`/raw strings, mirroring `CharacterState.saving_throw_proficiencies`/`skill_proficiencies`. The empty default preserves every existing `CreatureState(...)` call site. `conditions` is authoritative **effective membership** of the current supported Condition set, not a collection of runtime Condition-instance objects; `condition_NNN` remains reserved for a possible future stateful Condition-instance model (its own source/duration/provenance lifecycle) introduced only if concrete mechanics require it, and no runtime allocation of that ID format is introduced here. `StateSnapshot`'s top-level shape (`campaign`/`creatures`/`characters`, §3.2.3) is unchanged; no `StateSnapshot.conditions` field or `ConditionState` aggregate is added. State schema gains exact integer `schemaVersion = 4` as the current writer, alongside new explicit `LEGACY_SCHEMA_V3_VERSION = 3` (next to the existing `LEGACY_SCHEMA_VERSION = 1`/`LEGACY_SCHEMA_V2_VERSION = 2`); V4 Creature JSON always includes `conditions` (a JSON list of exact strings, each mapping to a known `Condition`, no duplicates, sorted deterministically by `Condition.value` on write), while V1–V3 Creature payloads keep the pre-existing fixed field set with `conditions` unknown/forbidden and always decode to `CreatureState.conditions == frozenset()`. The Character `skillProficiencies` decoding branch in `StateSerializer._deserialize_character` is changed from comparing against `SCHEMA_VERSION` to comparing against `LEGACY_SCHEMA_V2_VERSION` (`schema_version != LEGACY_SCHEMA_V2_VERSION`), so V3 and V4 both decode the unchanged Character schema correctly regardless of future `SCHEMA_VERSION` bumps — Character semantics are otherwise byte-for-byte identical between V3 and V4. A separate fixed constant, `SCHEMA_V4_VERSION = 4` (with `SCHEMA_VERSION = SCHEMA_V4_VERSION` today), is introduced specifically for V4 Creature-shape identity: the supported-schema-version set, the V4 Creature field set, and `conditions` decoding all compare against `SCHEMA_V4_VERSION`, never against the mutable `SCHEMA_VERSION`. `SCHEMA_VERSION` is retained solely to denote "whatever the writer currently emits" (used only when serializing); it is decoupled from every place that decides historical V4 read semantics, closing the exact same class of schema-evolution trap this decision already closes for Character `skillProficiencies`. A regression test monkeypatches `SCHEMA_VERSION` to a hypothetical future value and asserts a historical V4 payload with `conditions` still decodes correctly.
- **Consequences:** The repository has a persisted Condition representation with strict encode/decode and full backward-compatible legacy reads, but no Condition mutation, Event, or gameplay behavior yet — Apply/Remove Commands, `ConditionApplied`/`ConditionRemoved` Events, Application handlers, and the `Poisoned` effect itself remain deferred to later G6C groups. No Command, Event, `ResolutionResult`, `StateStore` signature, or `StateSnapshot` shape changed. Broad Roadmap `Conditions` stays unchecked because this slice covers only the State foundation, not the Definition of Done for the mechanic. The `SCHEMA_VERSION`-equality regression this decision closes — in `_deserialize_character` for Character `skillProficiencies`, and in `_deserialize_creature` for the V4 Creature field set/`conditions` — is a narrow, backward-looking correctness fix scoped to this slice, not a new abstraction; it changes no observable V1–V3 behavior and introduces no schema/version framework.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.2.1, 3.21, 4.12, 12.9, 12.12; `docs/DECISIONS.md`; `docs/DEVELOPMENT_LOG.md`; `CLAUDE.md`; `src/dnd_engine/domain/value_objects/condition.py` (new); `src/dnd_engine/domain/state/creature.py`; `src/dnd_engine/infrastructure/persistence/json/state_serializer.py`; `tests/domain/test_condition.py` (new); `tests/domain/test_creature_state.py`; `tests/infrastructure/test_state_serializer.py`; `tests/infrastructure/test_state_store.py`.

## DEC-0036 — Condition Apply/Remove pure Domain mutation contract (G6C1, Group 2)

- **Date:** 2026-08-28
- **Status:** Accepted
- **Context:** DEC-0035/§3.21 fixed the persisted representation of Conditions (`Condition` identity, `CreatureState.conditions`, State schema V4) but explicitly deferred every mutation concept — Apply/Remove Commands, Events, and appliers — to a later group. §3.18's State Mutation Foundation already fixes the canonical contract a state-mutating Command must follow, and G6A (DEC-0033)/G6B (DEC-0034) already supply two concrete precedents for the Command → pure resolver → Event → concrete Creature applier split, with Application orchestration and persistence kept as a separate later step. This group supplies that same pure Domain mutation contract for Conditions, without yet touching Application handlers or `StateStore.save()`.
- **Decision:** Add `ApplyConditionCommand`/`ApplyConditionPayload` (`src/dnd_engine/domain/commands/apply_condition.py`) and `RemoveConditionCommand`/`RemoveConditionPayload` (`.../remove_condition.py`), each an immutable dataclass with payload `(target_id: str, condition: Condition)` and no `source`/`duration`/`save_dc`/`spell_id`/`item_id`/`feature_id`/`stacks`/`condition_instance_id`, matching the existing Command Envelope shape. Add two concrete result types — `ConditionApplicationResult`/`ConditionRemovalResult` (`domain/rules/apply_condition.py`/`remove_condition.py`), each `(target_id, condition, previous_active, active)` — because no existing shared "membership transition" type was found; `active` is intrinsically fixed by each type's own `__post_init__` (`True` for Application, `False` for Removal), so a Result with the opposite endpoint cannot even be constructed. `resolve_condition_application`/`resolve_condition_removal` are pure: `previous_active = condition in target.conditions`, no mutation, no I/O, no `DiceEngine`, no Definition lookup, no duration/source/stacking decision, and the same target-identity correlation check already used by `resolve_damage`/`resolve_healing`. Applying an already-active Condition or removing an already-absent one is an explicit **successful no-op** (`RULE_VIOLATION` is not used), and it is not short-circuited anywhere on the Domain Event path — the resolver still returns a normal result and the builder still constructs a full Event. Add `ConditionApplied`/`ConditionRemoved` V1 Events (`domain/events/apply_condition.py`/`remove_condition.py`) with payload exactly `{targetId, condition, previousActive, active}`; `build_condition_applied_v1`/`build_condition_removed_v1` check Command/outcome correlation and copy the already-resolved result verbatim, matching `build_damage_applied_v1`/`build_healing_applied_v1`. The Event payload dataclasses reuse the same fixed-`active`-invariant pattern as their Result counterparts, so a malformed Event with the opposite endpoint is rejected the same way, both from the builder and from raw/decoded reconstruction. Add concrete Creature appliers `apply_condition_applied_v1`/`apply_condition_removed_v1`, following the existing `apply_damage_applied_v1`/`apply_healing_applied_v1` integrity-check shape (exact Event type/version/payload keys, decoded `targetId`/`condition`/`previousActive`/`active`, `targetId == creature.id`, and the Condition-specific check `previousActive == (condition in creature.conditions)` — a mismatch is a stale/integrity `ValueError`, not a gameplay `EngineError`) before projecting `conditions | {condition}` (Apply) or `conditions - {condition}` (Remove) through `dataclasses.replace`; only `conditions` changes, `id`/`definition_id`/`ability_scores`/`current_hp`/`max_hp` are preserved. Exactly like G6A's `0 → 0` Damage and G6B's `maxHp → maxHp` Healing, a canonical no-op Event (`previousActive`/`active` both `true`, or both `false`) can be re-applied to a Creature whose membership already matches this endpoint, because no State revision, CAS, or applied-Event-ID registry exists; this is a documented limitation, not fixed here via `revision`, an `EventStore`, a processed-command registry, or a generic Event-application registry.
- **Consequences:** The repository has a complete, tested pure Domain mutation contract for Condition membership — Command → resolver → Event → Creature applier — without any Application handler, `StateStore.save()` call, or gameplay effect. `ApplyConditionHandler`/`RemoveConditionHandler`, persistence orchestration, `Poisoned` disadvantage/RollMode interaction, `ModifierPipeline`, a generic Event applier, a generic mutation handler, `EventStore`, `UnitOfWork`, `WorkingState`, and `ResolutionResult.state_changes` all remain deferred, matching §§3.6/3.18. No `ErrorCode`, `StateStore` signature, `StateSnapshot` shape, or existing Command/Event/Result contract changed. Broad Roadmap `Conditions` stays unchecked; this decision does not complete it and does not make its completion conditional on any later documentation group.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §3.21; `docs/DECISIONS.md`; `docs/DEVELOPMENT_LOG.md`; `CLAUDE.md`; `src/dnd_engine/domain/commands/apply_condition.py` (new), `.../remove_condition.py` (new); `src/dnd_engine/domain/rules/apply_condition.py` (new), `.../remove_condition.py` (new); `src/dnd_engine/domain/events/apply_condition.py` (new), `.../remove_condition.py` (new); corresponding new tests under `tests/domain/`.

## DEC-0037 — Minimal Poisoned behavior uses mechanic-specific Condition policies (G6C2)

- **Date:** 2026-08-29
- **Status:** Accepted
- **Context:** G6C1 made `CreatureState.conditions` authoritative persisted membership and implemented Apply/Remove persistence, but deliberately left membership without gameplay behavior. The existing Ability Check, Character Skill Check, Character Saving Throw, and Character unarmed Attack resolvers already accept an effective keyword-only `RollMode`; Application owns authoritative Creature lookup. The first real Condition effect is dnd_5e Poisoned: disadvantage on Ability Checks and Attack Rolls. In the current engine Skill Check is a concrete Ability Check mechanic, while Saving Throw is not affected. Poisoned is also the only real production advantage/disadvantage source, so there is no evidence for source aggregation yet.
- **Decision:** Implement exactly two pure, context-specific Domain policies in `domain.rules.condition_roll_mode`: `ability_check_roll_mode_from_conditions()` and `attack_roll_mode_from_conditions()`. Each maps authoritative Condition membership to `DISADVANTAGE` when `POISONED` is present and `NORMAL` otherwise. Skill Check reuses the ability-check policy; Saving Throw invokes neither policy. AbilityCheckHandler, SkillCheckHandler, and AttackHandler read the already-loaded actor/attacker `CreatureState.conditions`, obtain effective mode from Domain, and pass it to the unchanged resolver boundary. Attack uses attacker Conditions, never target Conditions. Commands/API/UI/AI do not carry or determine roll mode. Existing resolvers continue delegating actual rolling to `resolve_d20_roll()`, and Event writers continue recording the real `D20Roll` mode, ordered rolls, and selected value. Do not introduce a generic `Condition -> RollMode` rule, `combine_roll_modes`, pairwise combiner, source/context/modifier pipeline, or effect registry/hierarchy. When a second real advantage/disadvantage source appears, first design a representation of the presence of independent advantage and disadvantage sources and only then derive final `RollMode`; do not combine already-collapsed modes pairwise.
- **Consequences:** Persisted `POISONED` membership now causes two independent `"1d20"` calls and lower selection for Ability Checks, Skill Checks, and Attack Rolls. Poisoned Saving Throws still perform one `"1d20"` call with `RollMode.NORMAL`. Existing resolver signatures, Commands, Events, State schema, Monster AC lookup, Attack natural 1/20 semantics, and State mutation paths are unchanged. No other Condition or Poisoned behavior is implemented; broad Roadmap `Conditions`, Attack, Skills, and Proficiency statuses remain unchanged.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.12, 3.21–3.22; `docs/ROADMAP.md`; `CLAUDE.md`; `docs/DEVELOPMENT_LOG.md`; `src/dnd_engine/domain/rules/condition_roll_mode.py`; `src/dnd_engine/application/handlers/ability_check.py`; `skill_check.py`; `attack.py`; focused Domain/Application/integration tests.

## DEC-0038 — Post-G6C review extracts only stable Creature snapshot replacement

- **Date:** 2026-08-29
- **Status:** Accepted
- **Context:** DEC-0034's post-G6B review retained all mutation candidates as concrete because only Damage and Healing existed. G6C added Apply/Remove Condition as two more production mutation handlers. All four now independently repeated the same complete Application-owned block after their concrete Event applier returned a replacement `CreatureState`: replace by stable Creature ID, preserve tuple order, reuse Campaign and Character projections, leave the loaded snapshot untouched, and save the new snapshot. `StateSnapshot` already prohibits duplicate Creature IDs. The remaining failure case—an unknown replacement ID—was implicit at the call sites and would otherwise yield a silently unchanged snapshot. In contrast, the four Event appliers still have materially different payload decoding and integrity contracts, and the read-only Condition consumers still have mechanic-specific policy.
- **Decision:** Extract one Application helper, `replace_creature_in_snapshot(snapshot, replacement)`, with the narrow §3.23 contract. It validates `StateSnapshot`/`CreatureState` inputs, requires `replacement.id` to match exactly one existing Creature, raises `ValueError` instead of appending or silently preserving on a missing ID, constructs a new tuple in the original order, and reuses the exact Campaign and characters tuple. Migrate `DamageHandler`, `HealingHandler`, `ApplyConditionHandler`, and `RemoveConditionHandler` to this helper. The helper performs no lookup, authorization, gameplay resolution, Event construction/decoding/application, field mutation, persistence, transaction coordination, or dispatch. Keep concrete the Creature field transitions, all four Event appliers, and all four handlers' broader orchestration; their shared sequence does not justify callbacks, generics, error factories, hooks, a Protocol/base applier, registry, or reducer. Keep the ability-check and attack Condition policies narrow, keep Skill reuse and Saving Throw exclusion, and do not add `has_condition`, a generic effect/mechanic framework, or RollMode aggregation. Continue to defer `WorkingState`, `MutationContext`, `UnitOfWork`, `TransactionManager`, `state_changes`, `EventStore`, `EventApplierRegistry`, replay/reducer infrastructure, revision/CAS/idempotency, and advantage/disadvantage source representation until their stated production evidence appears.
- **Consequences:** Snapshot replacement failure and preservation semantics now have one tested Application contract and four handler regressions, while Domain gameplay/Event contracts and the `StateStore.load`/`save` port remain unchanged. This is the only new production abstraction in the post-G6C review and it supersedes only DEC-0034's snapshot-helper verdict; DEC-0034's other `KEEP CONCRETE` conclusions remain accepted. No gameplay behavior, schema, dependency, API/UI authority, Roadmap completion status, or persistence resource was added.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.19–3.23; `docs/DECISIONS.md`; `docs/DEVELOPMENT_LOG.md`; `CLAUDE.md`; `README.md`; `AI_DND_DATA_FLOW_CURRENT.md`; `src/dnd_engine/application/services/state_snapshot.py`; the four mutation handlers; `tests/application/test_state_snapshot_service.py`; the four handler test modules as regression coverage.

## DEC-0039 — Phase 2 closes on foundation readiness with linked forward scope

- **Date:** 2026-08-30
- **Status:** Accepted
- **Context:** Phase 2 Roadmap labels such as `Proficiency`, `Saving throws`, `Attack rolls`, `HP`, `Damage`, `Healing`, and `Conditions` were broader than the deliberately narrow implemented slices. An unchecked broad label could mean either that no reusable foundation existed or that a tested foundation existed while later variants, sources, consequences, or lifecycle consumers remained absent. That ambiguity made implemented readiness look incomplete and made deferred scope look capable of holding Phase 2 open indefinitely.
- **Decision:** Close Phase 2 when its reusable deterministic Basic Rules foundations are sufficient for later concrete consumers, as specified by Architecture §3.24; do not require full D&D mechanic coverage. Mark the scope-accurate foundation items complete in Roadmap and label every still-incomplete broader mechanic `broader scope PARTIAL`. Each partial Phase 2 item must link forward to its detailed `P2-*` closure note and stable `DEF-*` continuation records in `docs/DEFERRED.md`; the relevant future Roadmap location must link back to those records so ownership is visible in both directions. Attack consequence belongs to the concrete Phase 3 attack consumer that first needs to connect a resolved hit to Damage. Character and Monster zero-HP legality belongs to concrete Phase 3 Turns/action-eligibility consumers and is evaluated separately rather than requiring a universal life-state model. Durable Event History/Replay remains a trigger-driven cross-cutting track: it starts when a real persistence, recovery, decoding, replay, or idempotency consumer exists and is not a Phase 3 entry gate.
- **Consequences:** Phase 2 — Basic Rules is complete for foundation scope, and Phase 3 — Combat becomes current. Advanced proficiency and Monster variants, weapons/spells, Attack → Damage, critical damage, zero-HP/Death Save rules, temporary HP, typed defenses, source-aware recovery, full Conditions/effects, roll-source aggregation, EventStore/replay, and similar registered work remain unimplemented without reopening Phase 2. Roadmap remains the status/order source, Architecture remains the current contract, Decisions remains append-only rationale/history, and Deferred remains subordinate planning detail. The demand-driven/evidence-driven abstraction policy is unchanged: a DEF record or phase transition does not authorize generic Effects, modifier/mutation pipelines, replay/transaction frameworks, or other abstractions before concrete production evidence.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.8, 3.18, 3.23–3.24; `docs/ROADMAP.md`; `docs/DEFERRED.md` (introductory closure-status wording synchronized); `README.md`; `CLAUDE.md`; `AI_DND_DATA_FLOW_CURRENT.md`; `docs/DEVELOPMENT_LOG.md`.

## DEC-0040 — First Phase 3 Combat consumer: dice-rolled Initiative/Turn Order with a narrow actor-eligibility gate (G7)

- **Date:** 2026-08-30
- **Status:** Accepted
- **Context:** Phase 2 closed on foundation readiness (DEC-0039); Architecture §3.24 explicitly assigned "Attack-consequence and zero-HP action-eligibility rules" to "the first concrete Phase 3 Combat consumers that need those facts," and Roadmap Phase 3 opened with `Initiative`, `Turns`, and `Zero-HP and combatant eligibility` as its first unchecked items. §10.7 already named `CombatEngine` as the owner of combat lifecycle, participants, initiative, round, turn, and active combatant, but no concrete `CombatState`, Command, Event, or Application handler existed yet. The task was to deliver the smallest vertical slice that both models Initiative/Turns as real persisted State and exercises a genuine actor/action-eligibility check, without inventing zero-HP/Death Save/life-state machinery, a generic action pipeline, or a new transaction abstraction ahead of evidence. A pre-commit review pass on this same uncommitted slice additionally found: `StartCombatHandler` did not validate `command.actor_id` against authoritative State before acting, matching the actor-first validation every other handler already performs; SRD 5.1 Initiative is a Dexterity check, so the already-implemented Poisoned Ability Check Condition policy (§3.22) should already apply to it, but the first implementation hard-coded `RollMode.NORMAL` and excluded Poisoned; and `StartCombatPayload.combat_id`'s Architecture wording read as though an arbitrary caller could mint a Combat ID, in tension with §4.11's ID Generation Policy (Entity IDs are generated only by `EntityFactory`).
- **Decision:** Add a new minimal State Owner type, `CombatState(id, round, order, active_index)` (§3.25), and exactly one new optional `StateSnapshot.combat: CombatState | None = None` field (defaulting to "no combat in progress," so no existing snapshot construction site is affected). Add two Commands following the unchanged §3.18 mutating-command lifecycle: `StartCombatCommand(combat_id, participant_ids)` and `AdvanceTurnCommand(combat_id)`. `StartCombatHandler` first validates `command.actor_id` against authoritative `snapshot.creatures` — a missing actor returns `ENTITY_NOT_FOUND` (`entity_id=actor_id`, `field=None`) before participant resolution, any `DiceEngine` call, any `EventMetadataProvider` call, or persistence, matching the existing actor-first validation order used by `AttackHandler`/`DamageHandler`/`ApplyConditionHandler`; nothing requires the actor to also be a participant. Only then does it reject starting combat while one is already in progress (`RULE_VIOLATION`) and report a missing participant as `ENTITY_NOT_FOUND`. Initiative is a Dexterity check (SRD 5.1): `StartCombatHandler` derives each participant's effective `RollMode` from authoritative `CreatureState.conditions` via the existing `ability_check_roll_mode_from_conditions` (§3.22) — the same function `AbilityCheckHandler`/`SkillCheckHandler` already call — and passes the resulting per-participant `tuple[RollMode, ...]` into `resolve_start_combat(command, participants, dice, *, roll_modes)`, a new required keyword-only parameter positionally aligned with `participants`. The resolver still owns all Dexterity-check gameplay math: it rolls via `resolve_d20_roll(dice, roll_mode)` (§3.12) per participant — one `"1d20"` call under `RollMode.NORMAL`, two independent calls under `RollMode.DISADVANTAGE` — adds the shared `ability_modifier` (§1.2.1), and sorts by descending total, then descending raw Dexterity, then ascending creature ID. This is reuse of an existing closed Domain policy, not a new Condition/effect framework: no `combine_roll_modes`, independent-source aggregation, or generic Condition→RollMode mapping was introduced, and DEF-0021 remains untouched (Poisoned is still the only production roll-mode source). `AdvanceTurnHandler` requires `command.actor_id == combat.active_creature_id` before resolving, returning `ACTION_NOT_AVAILABLE` otherwise; this actor-ownership check — not a change to `AttackHandler` — remains the concrete "actor/action-eligibility consumer" this slice delivers, and `current_hp` is deliberately still not consulted, leaving DEF-0015's zero-HP eligibility question exactly as open as before. Both handlers attach/replace `StateSnapshot.combat` with the stdlib `dataclasses.replace(snapshot, combat=...)` directly, because a single optional field needs no by-ID tuple search — the existing §3.23 `replace_creature_in_snapshot` helper does not apply, and no analogous helper is extracted for it. Duplicate creature IDs are now rejected at every layer that could otherwise let one slip through: `StartCombatPayload.participant_ids` already rejected duplicate input IDs; `StartCombatResult` now also rejects a duplicate creature ID in its own resolved aggregate `order`; `CombatState` enforces the same invariant at construction; and the State serializer's `_validate_combat` re-validates it again before V5 persistence, because `CombatState` is a mutable dataclass and could have been mutated after construction. `InitiativeEntry` itself carries no duplicate-ID responsibility. State schema gains exact integer `schemaVersion = 5` (`SCHEMA_V5_VERSION`, following the same fixed-sentinel-vs-mutable-`SCHEMA_VERSION` discipline DEC-0035 established) as the current writer, adding exactly one required top-level `state.combat` key (`null`, or an object with `id`/`round`/`order`/`activeIndex`); V1–V4 payloads keep their existing field sets with no `combat` key and always decode to `combat=None`. `combat_id` on `StartCombatPayload` is documented as an **already-allocated** runtime Combat ID produced through the canonical Entity-ID generation boundary (§4.11 `EntityFactory`) before the Command is constructed — §4.11 itself is unchanged, and this slice does not implement a concrete `EntityFactory`; UI/AI still never invent a `combat_NNN`. `AttackCommand`/`AttackHandler` (§3.17) are completely unchanged.
- **Consequences:** Phase 3 gains its first real Combat State Owner, a genuine actor-eligibility gate (both the `StartCombat` actor-existence check and the `AdvanceTurn` turn-ownership check), Poisoned-aware Initiative reusing an existing closed Condition policy, and full production persistence evidence (round-trip through the real `FilesystemStateStore`), without introducing `CombatEnded`, Death Saves, zero-HP/unconscious semantics, movement, reactions, turn/action economy, grouped/identical-creature Initiative, a generic action/eligibility pipeline, `UnitOfWork`/`TransactionManager`, or independent-source RollMode aggregation. Roadmap records scope-accurate `Initiative foundation` and `Turn-order advancement foundation` rows rather than blanket `Initiative`/`Turns` completion, with explicit open rows for grouped Initiative, turn/action economy, `CombatEnded`, and `Zero-HP and combatant eligibility`. No existing Command, Event, `ResolutionResult`, `ErrorCode`, or `StateStore` contract changed; `StateSnapshot` gained exactly one optional field, and `resolve_start_combat` gained one required keyword-only parameter.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.2.3, 3.8, 3.18, 3.25, 12.9, 12.12; `docs/ROADMAP.md`; `CLAUDE.md`; `docs/DEVELOPMENT_LOG.md`; `README.md`; `AI_DND_DATA_FLOW_CURRENT.md`; `src/dnd_engine/domain/state/combat.py` (new); `src/dnd_engine/domain/state/snapshot.py`; `src/dnd_engine/domain/commands/start_combat.py`, `.../advance_turn.py` (new); `src/dnd_engine/domain/rules/start_combat.py`, `.../advance_turn.py` (new); `src/dnd_engine/domain/events/start_combat.py`, `.../advance_turn.py` (new); `src/dnd_engine/application/handlers/start_combat.py`, `.../advance_turn.py` (new); `src/dnd_engine/infrastructure/persistence/json/state_serializer.py`; corresponding new/updated Domain/Application/integration tests and updated `tests/infrastructure/test_state_serializer.py`/`test_state_store.py`.

## DEC-0041 — First Monster attacker, first Character target: Goblin Scimitar (G8)

- **Date:** 2026-08-30
- **Status:** Accepted
- **Context:** Phase 2 closure (DEC-0039) and G7 (DEC-0040) left Attack-consequence work (DEF-0013) explicitly gated on "the first concrete Phase 3 Combat consumer" (satisfied by G7) and "a real Weapon attack or Monster attack consumer" (DEF-0011/DEF-0012), neither of which existed: the only implemented Attack was the unarmed Character→Monster slice (§3.17), Monster was never an attacker, and Character was never a target. An initial exploration proposed a `weapon_definition_id` field directly on a new `WeaponAttackCommand` as the minimal Weapon-attack unblocker; this was rejected because a caller-supplied Definition ID is not an authoritative ownership fact (DEF-0011 already names a minimal Equipment/Inventory source, real weapon proficiency, and an explicit — not auto-maximized — Finesse choice as real prerequisites, and `AttackCommand` should stay one intent per its own "Planned approach" wording, not gain a sibling Command). A full correct Weapon-attack prerequisite was found to require two new State Owners (`InventoryState`, `EquipmentState`), a new `CharacterState.weapon_proficiencies` field, and two `AttackPayload` extensions — disproportionate for unblocking DEF-0013 alone. A Monster-attack alternative was compared and found to need zero new State Owners, because a Monster's attack is fully self-contained in its (already-existing-pattern) Definition. The chosen slice makes the Monster attacker target a Character specifically (not another Monster), so it simultaneously proves the Character-target part of DEF-0011's own "Weapon attacks and Character-target attacks" scope, chosen here as the "relevant part of DEF-0011" that DEF-0013's own prerequisite list already names generically (DEF-0013 does not itself name "the Character-target part" specifically, nor does DEF-0011 itself name "a concrete damage source" as one of its own prerequisites — that phrasing belongs to DEF-0013's separate, second prerequisite), using the already-implemented `unarmored_character_armor_class` (§3.15) rather than inventing a new AC source.
- **Decision:** Add `MonsterAttackDefinition(action_id, name, attack_bonus, damage_dice, damage_modifier, damage_type)` (§3.26) as a narrow attack-specific nested value type — not a generic Monster action/ability model, not a `Definition` subtype, and not paired with a `MonsterDefinition.actions` field of mixed action kinds. `action_id` is a local identifier scoped to the owning `MonsterDefinition`, never a runtime `action_NNN` Instance ID and never a globally registered identity, and is not added to the §4.13 ID Reference Table; no Action registry is introduced. `action_id` must match the canonical local format `^[a-z][a-z0-9_]*$` (lowercase snake_case), validated by a small Domain-local regex owned by `MonsterAttackDefinition` itself rather than the Infrastructure packaged-resource-segment pattern (§12.26) imported into Domain. `MonsterAttackDefinition.__post_init__` validates every field intrinsically, reusing the shared `parse_ndm()` for `damage_dice` (the same primitive `WeaponDefinition.damage_dice` already uses); `MonsterDefinition.__post_init__` only additionally checks `attacks` is a tuple of `MonsterAttackDefinition` values with unique `action_id`s, not re-validating fields the nested type already owns. `MonsterDefinition.attacks: tuple[MonsterAttackDefinition, ...] = ()` defaults to empty, preserving every existing construction site; zero attacks is a valid Definition, not an error. The packaged `goblin` Definition gains exactly one attack, the SRD 5.1 Scimitar (`+4` to hit, `1d6 + 2` slashing); the Goblin's Shortbow is deliberately not packaged (ranged/reach fields do not exist on this contract yet), and `goblin.version` stays `1` because `PackagedDefinitionSource` has no Definition-version-aware lookup and this is the project's minimal representation of already-published SRD content catching up to a new contract field, not a change to an already-relied-upon rule value. `AttackCommand`/`AttackPayload(target_id)` (§3.17) are completely unchanged — one explicit Attack intent, per DEF-0011's own planned approach. `AttackHandler` gains one new branch keyed on data it already loads: an actor with a matching `CharacterState` projection takes the existing unarmed path unchanged; an actor without one — previously an unconditional `INVALID_STATE` failure — now takes a new Monster-actor path, which requires the actor's `MonsterDefinition` to have exactly one `MonsterAttackDefinition` (`ACTION_NOT_AVAILABLE`, not `INVALID_STATE`, for zero or multiple — the Definition stays valid, this narrow Command just cannot select among several), requires the target to have a `CharacterState` projection (`INVALID_TARGET` otherwise — Monster→Monster remains unimplemented), computes target AC via the unchanged `unarmored_character_armor_class`, and derives `RollMode` via the unchanged `attack_roll_mode_from_conditions`. A new pure resolver `resolve_monster_attack` and a new `MonsterAttackResult(target_id, action_id, roll, attack_bonus, total, target_armor_class, hit, critical_hit)` are added as a separate concrete type from `AttackResult`, not a variant of it: a flat stat-block `attack_bonus` is not decomposed into a fabricated `ability`/`ability_modifier`/`proficiency_bonus` split, because that split does not describe how the number actually arose. `AttackHandler.handle()`'s return type becomes `ResolutionResult[AttackResult | MonsterAttackResult]`. A new Event, `MonsterAttackResolved` V1 (`targetId, actionId, roll, attackBonus, total, targetArmorClass, hit, criticalHit`), is added rather than an `AttackResolved` V2, because the field set is conceptually different (not just a roll-representation change, unlike the `AbilityCheckResolved` V1→V2 precedent) — matching why `SkillCheckResolved`/`SavingThrowResolved`/`AbilityCheckResolved` already stay separate Event types despite a shared d20 foundation. `AttackResolved` V1 is completely untouched. The Event carries no damage fields and no `previousHp`/`newHp`: it records the attack roll only, exactly like `AttackResolved`; Damage resolution is explicitly left to a separate DEF-0013 Event. `MonsterAttackResult`/`MonsterAttackResolvedPayloadV1` duplicate `AttackResult`/`AttackResolvedPayloadV1`'s natural-1/20 and `total >= target_armor_class` invariant checks; per the existing evidence-driven abstraction policy (§3.6), a second concrete consumer does not by itself justify extracting a shared natural-outcome helper, and none is introduced.
- **Consequences:** The repository has its first Monster-as-attacker and Character-as-target Attack path, backed by production packaged data and a full `FilesystemStateStore`/`PackagedDefinitionSource`/`PythonDiceEngine` round trip, without introducing Inventory/Equipment State, Character weapon proficiency, Finesse choice, a `MonsterAttackCommand`, a generic Monster action/effect framework, an Action registry, range/reach/multiattack/recharge/saving-throw actions, Damage resolution, or `causedBy` chains. This slice evidences the Character-target part of DEF-0011 and supplies a concrete Monster damage source (`damage_dice`/`damage_modifier`/`damage_type`, unused by this slice) for a future DEF-0013 Attack-consequence slice, but does not implement DEF-0013 itself and does not redefine its stated prerequisites. The same landed `attack_bonus` also satisfies every acceptance criterion DEF-0002 (Monster proficiency sources) already named for its own foundation: at least one Monster consumer resolving from typed authoritative data, missing/wrong-type Definitions following the existing lookup-error contract, no incorrect reuse of Character formulas, and packaged/deterministic test coverage — DEF-0002 closes `Done` on that foundation, while Monster Saving Throw proficiency, Monster Skill proficiency, a generic Monster proficiency formula, and challenge-rating-derived proficiency remain unimplemented and stay the separately-tracked concern of DEF-0004. Roadmap gains a scope-accurate `Monster → Character attack-roll foundation` row that also records DEF-0002's closure; broad `Monster actions` and `Weapon attacks` remain open, and DEF-0004/DEF-0011/DEF-0012 remain `Deferred` for their remaining scope with a dated History entry recording this partial foundation. No existing Command, `ErrorCode`, `StateStore`, or State schema contract changed; `AttackResult`/`AttackResolved` V1 are unchanged, and `AttackHandler`'s outcome type gained a union member.
- **Affected contracts/files:** `docs/ARCHITECTURE.md` §§3.1.1, 3.26; `docs/DECISIONS.md`; `docs/ROADMAP.md`; `docs/DEFERRED.md` (DEF-0002, DEF-0004, DEF-0011, DEF-0012, DEF-0013); `docs/DEVELOPMENT_LOG.md`; `CLAUDE.md`; `README.md`; `AI_DND_DATA_FLOW_CURRENT.md`; `src/dnd_engine/domain/definitions/monster_attack.py` (new); `src/dnd_engine/domain/definitions/monster.py`; `src/dnd_engine/domain/rules/monster_attack.py` (new); `src/dnd_engine/domain/events/monster_attack.py` (new); `src/dnd_engine/application/handlers/attack.py`; `src/dnd_engine/infrastructure/definitions/packaged.py`; `src/dnd_engine/resources/rulesets/dnd_5e/5.1/definitions/goblin.json`; `src/dnd_engine/resources/rulesets/dnd_5e/5.1/NOTICE.md`; corresponding new/updated Domain/Application/integration tests.
